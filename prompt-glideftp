prompt glide-ftp :

Le nom de l'application sera "GlideFTP"

Je souhaite faire un client ftp en go sousforme d'application en utilisant "wails" (la v2 celle qui est stable : github.com/wailsapp/wails/v2/cmd/wails@latest)

Le but est que l'application soit compatible avec Windows et Linux

Je t'ai fourni une esquisse du front que je veux (./_images/exemple.png)

Il faut absolument un light theme et un darktheme (le dark theme doit etre celui par defaut, il doit etre changeable dans les paramètres) + il faut un choix de langue entre francais et anglais (l'anglais doit etre la langue par defaut mais l'utilisateur pourra changer à sa guise la langue par defaut dans les paramètres) 

L'appli doit prendre en compte les clé ssh (SFTP)

A coter du bouton "transférer" (à droite) tu dois rajouter un bouton file d'attente afin de faire apparaitre une file d'attente en bas de l'application qui auront 3 onglets : File d'attente, Transferts échoués, Transferts réussis

A droite de l'input "Port" il faudra un bouton avec un logo de connexion pour lancer la connexion (elle est absente sur le screen que je t'ai envoyé)

Pour les sites enregistrées -> Quand on clique sur "Gérer les sites" Il faut qu'on pop-up apparaisse au milieu de l'écran avec la possibilité de créer des sites et de renseigner toutes les informations nécessaires comme le protocole utilisé (ftp ou sftp, il faudra faire un petit "i" d'information quand l'utilisateur passe sa souris dessus pour expliquer rapidement les 2 protocoles), l'hote, le port, le type de chiffrement, le type d'authentification (Anonyme,Compte, Demander le mot de passe, Interactif et Normale)

Pense à compléter les paramètres avec tous les paramètres necessaire, attention il faut que la fenetre des paramètre soit plus grande que sur les screen que je t'ai envoyé il faudrait que ca prenne les 3/4 de la page de droite à gauche)

Maintenant fait moi réflechir a comment penser mon application stp

________

FRONT : 

Je dois faire un client (application en dur) FTP et j'aimerais que tu me dessine une esquisse (de plusieurs photo/exemple) d'une interface client ftp stp
les input hote, user, mdp, port et le bouton connexion doivent etre au milieu de la page si aucune connexion n'est active sinon elle doivent etre en haut au milieu 
Un bouton avec une roue cranté (paramètres) doit etre en haut à droite et quand on clique dessus cest un menu coulissant qui vient prendre les 3/4 de la page de droite à gauche 
Le site local (l'hote) doit être sur la gauche de la page une fois la connexion établie et le site distant (le nas par exemple) doit être à droite
Les options rapide comme "se deconnecter" ou "actualiser" seront en dessous des input 
Une icône/bouton doit etre à gauche de celui de la roue crantée pour les paramètres qui servira à enregistrer des sites pour s'y connecter ensuite

_________

ISSUES :

1.0

1 = Le Front de la page de connexion n'est pas correct --- regarde dans ./_images/issues/issue-1.png

2 = quand sur l'écran de connexion (quand je lance l'appli, je créer un nouveau sites et que je clique ensuite dessus puis sur "connecter" le logiciel reste sur la page de connexion sans m'afficher les fichiers distants ou local et j'ai l'erreur : "already connected or connecting" de JavaScript - wails://wails/ --- regarde dans ./_images/issues/issue-2.png et issue-2.1.png

3 = Quand je met mon application en grand elle ne s'adapte pas à l'écran mais reste bloquer à une taille maximum --- regarde dans ./_images/issues/issue-2.png et issue-3.png

4 = Une fois connecté le bandeau en haut n'est pas bien aligner on ne vois pas certaine boutons qui sont au finale derirere une bannierre --- regarde dans ./_images/issues/issue-4.png

5 = Pour les sites enregistrés il faut rajouter un input avec une note dessous "Répertoire distant par défaut"

6 = une fois connecté et qu'on accès au fichier il faudrait avoir un dossier ".." pour pouvoir faire un retour arriere (il sera là en plus de la fleche que tu as deja créer ca c'est super)

7 = Le drag and drop pour transférer un fichier ne fonctionne pas

8 = Il n'y a pas la possibilité pour l'utilisateur de passer sa souris au milieu pour redimmensionner la taille de lhote et du serveur distant et pareil pour la file d'attente elle n'est pas réglable

9 = Quand on transfere un fichier l'actualisation des fichiers distants et en local ne se fait pas toute seul il faut obligatoirement cliquer sur le bouton actualiser, il faut que ca s'actualise tous seul

10 = Je ne peux pas faire un clique droit pourcréer un dossier par exmeple sur l'hote ou a distance, il faudrait aussi avoir la possibilité de faire un clique droit et -> transférer   

11 = Il n'y a pas la possibiliter de trier les fichiers par nom, taille ou date 

12 = je ne peut pas selectionner plusieur fichiers avec shift + clic ou ctrl + clic ni meme avec la souris en restant appuyer sur le bouton gauche

13 = Le raccourci F2 ne fonctionne pas pour renommer un fichier /dossier

14 = une fois connecter je ne peut plus aller dans paramètre et cocher l'option "afficher les fichiers cachés"

_____

1.0.1

15 = Sur l'appli je ne peux pas écrire moi même le chemin voulu en haut ni sur l'hote ni sur le serveur distant (exemple : /test/encore-test) 

16 = Une fois connecté le bndeau du haut est un peu "mordu" on le vois sur le logo rouge "déconnecter" --- regarde dans ./_images/issues/issue-5.png

17 = dans la file de transfert j'i vu ue je pouvais cliquer sur "vider" pour enlever tous les fichiers de transferts réussis mis je ne peut pas le fire un par un est-til possible de rendre cela possible en cliquant sur "done"

18 = J'ai remarque que beaucoup de paramètre ne fonctionne pas, je n'ai que la langue, le darktheme qui fonctionne tous le reste ne fonctionne pas ; par exemple je ne peut pas régler la valeur du port part défaut les fleche ne fonctionne pas et tout le reste ne fonctionne pas non plus j'ai aussi essayer de changer l'emplacement par defaut pour le mettre dans /home/mon-user/Téléchargements -> ca s'affich bien dans l'application mais lorsque je me reconnecte à mon serveur distant il continue de s'ouvrir sur /home/mon-user ; les boutons "on/off" ne fonctionne pass non plus comme "afficher les fichiers cachés"

_____

1.1

19 = c'est possible d'avoir une autocomplétion quand on tape le chemin manuellement en haut ?

20 = Peut tu rajouter un input "note" pour que l'utilisateur puisse noter ce qu'il veux sur chaque sites renseignée --- regarde dans ./_images/issues/issue-6.png

21 = Le bouton "suppr" ne fonctionne pas pour supprimer un fichier ou dossier ni en local ni sur l'hote distant, de plus fait en sorte de mettre un paramtre suplémentaire avec un bouton on/off pour donner le choix à l'utilisateur s'il veux une confirmation avant la suppression  d'un élément, pour la confirmation tu doit générer un pop-up qui sera présent si l'option est coché par l'utilisateur ; car pour le moment l'option est disponible dans les paramètres mais ne fonctionne pas

22 = Dans les options il faut ajouter une limite de transfert en Mo (cest au cas où un user n'a pas une bonne connexion)

23 = Le choix "demander le mot de passe" ne fonctionne pas quand j'ajoute un nouveaux site, le mot de passe reste enregistrer dans l'application

24 = Peut ton faire ne sorte de générer un export des sites enregistrés pour par exemple les importer sur un autre ordinateur sur GlideFTP ? (en json par exemple ?)

25 = Dans les paramètres dans l'onglet apparence est-ce qu'on peut faire ne sorte de générer un selecteur rgb pour que l'utilisateur puisse changer la couleur bleu qu'il y a par defaut, il faut que les valeurs rgb soit visible et changeable ainsi que hexadecimal, il faudra aussi un bouton "defaut" pour remmettre le bleu actuel, il faut que le nom soit "Couleur" ou "Color" en anglais et une fois qu'on clique sur l'icone un nouveau volet doit venir par dessus celui des parametres pour venir selectionner sa couleur et l'enregistrer --- regarde dans ./_images/issues/issue-rgb.png

26 = quand je reste appuyer sur le clic gauche de ma souris pour selectionner plusieurs fichiers/dossiers cela ne fonctionne pas

27 = J'ai vu que pour l'hote distant quand je clique sur les ".." que tu as créé avant, on dirait que la connexion entre l'hote et le serveur distant se coupe ; en tout cas je ne peux plus cliquer sur une dossier ou fichier comme si l'application ne répondait plus (si j me déco/reco ca refonctionne), pour info meme en cochant l'option "reconnexion automatique" cela ne fonctionne pas

_____

1.2

28 = Quand je selectionne plusieurs fichiers soit avec shift + clic soit avec ctrl + clic soit directement avec la souris en maintenant le clic gauche et que je drag and drop ma selection je n'ai que 1 seul fichiers qui est téléversé + quand je selectionne plusieurs fichiers et que je clique sur le bouton "transférer" (la petite fleche vers la droite) tout les fichiers se mettent en erreurs et si je les reprend 1 par 1 ensuite dans la laiste de transfert echoués et que je clique sur "réésayer" là ca fonctionne comme si plusieurs fichiers ne pouvais pas être transférés

29 = Pour le pop-up de mot de passe requis lorsuq'on se connecte est-ce qu'on peut rajouter un petit oeil pour pouvoir voir le mdp ou le caché

30 = quand je clique sur "annuler" dans la file d'attente j'ai l'impression que cela ne fait rien --- regarde dans ./_images/issues/issue-7.png

31 = J'ai vu que quand On selectionne SFTP il faut choisir le l'authentification "interactif" pour avoir la possibilité de choisir le clé ssh que l'on veut importer -> alors c'est très bien mais j'aimerais améliorer cela ; je veux que quand on selectionne l'authentification interactif alors le protocole se change automatiquement par du SFTP tout le reste selectionnera automatiquement FTP et si jamais l'utilisateur selectionne SFTP alors c'est l'authentification qui se changera automatiquement sur Interactif ainsi l'input de clé SSH apparaitra sans que l'utilisateur ne cherche le bon paramètre :) (je te laisse aussi gérer le backend comme il faut si cela n'est pas déjà fait)

_____

1.3

32 = Lorsque je selectionne plusieurs fichiers et que je veux les supprimer il n'y en a qu'un fichier qui est supprimer après avoir confirmer oui dans le pop-up

_____

1.4

33 = Les flèches et le bouton entrée ne fonctionne pas pour parcourir les dossiers/fichiers et entrée dedans

34 = Sur le 1er écran de connexion l'input hôte n'est pas assez grands il faudrait qu'il acceuil une IP v4 comme 192.168.124.155 (actuellement il n'acceuil que 3 octets)

35 = Pour le champs mot de passe requis quand j'essaye de me connecter via un site enregistré il faudrait avoir la possibilité de faire un clic droit puis coller

36 = Une fois connecter, en haut on peut voir les input hôte, user, mdp et port mais il on tous les valeurs par défaut, vil faut que ça soit rempli avec les vrais valeurs de l'utilisateur, sauf le mdp ou tu mettra 12 points qu'importe le mdp 

37 = Je ne sait pas si c'est déjà le cas mais il faut penser que si l'application est interrompu ou que l'utilisateur ferme la fenêtre alors que la connexion est toujours en cours il faut lancer une déconnexion comme si on avait déjà cliquer sur "déconnecter" 

38 = Il faudrait ajouter un historique de couleur de 8 couleurs 

_____

1.5

39 = J'ai constater un problème c'est que le transfert de mon hote vers mon nas fonctionne mais par contre quand j'essai de transférer de mon nas (serveur distant) vers mon hote alors le transfert reste à 0% --- regarde dans ./_images/issues/issue-8.png

40 = Alors tu as bien augmenter la taille de l'hote mais maintenant regarde dans ./_images/issues/issue-9.png tu verra que le port et la fleche de connexion sont passé en desous il faudrai qu'il soit toujours sur la même ligne comme avant, il faudrait que tu augmente légerement la taille du rectangle autour des inputs 

_____

1.6

41 = J'ai essayer de lancer le binaire qui est actuellement dans le dossier de build bin sur windows mais impossible de le lancer, comment on peut faire pour avoir un .exe de l'application comme GlideFTP.exe pour windows ? en terme d'arboresence il faudrait avoir ./build/bin/linux/GlideFTP et un autre dossier pour windows ./build/bin/windows/GlideFTP.exe ; je viens de voir qu'il y a une dossier windows dans ./build mais je ne sais pas à quoi il sert --- regarde dans ./_images/issues/issue-10.jpg

42 = Dans la file d'attente lorsque je transfert un fichier (ou dossier) on voit la taille du fichier et combien on été transféré mais on ne voit pas à combien de Mo/s on envoi (en temps réél) peut -on le faire apparaitre --- regarde dans ./_images/issues/issue-11.jpg

43 = est-ce qu'on peut ajouter un petit logo de copier au niveau des notes
  quand on veux se connecter via un site enregistrés stp --- regarde
  dans ./_images/issues/issue-12.jpg

44 = J'ai vu que tu avait bien implémenté le bouton copié mais il est mal centré --- regarde
  dans ./_images/issues/issue-13.jpg

45 = le ctrl + z ne fonctionne pas quand j'enregistre un site par exemple, peut tu faire en sorte que le ctrl + z annule la derniere action dans les inputs de GlideFTP stp

46 = j'ai remarqué que quand j'essai de me connecter en sftp via une clé .ppk cela ne fonctionne pas --- regarde dans ./_images/issues/issue-14.jpg et issue-15.jpg (ca cest la procédure que j'ai) je t'ai mis aussi la "key private.ppk" dans ./_images/issues

47 = J'ai remarqué que la taille de l'application ne pouvais pas excéder
  une certaine taille, par exemple je suis sur un ecran de 3440 * 1440
  (pixels) et l'application ne grandit pas autant elle est incapable de se
  mettre en grand écran --- regarde dans ./_images/issues/issue-16.jpg ========== NON OK sur Hyprland

____

1.7

48 = J'ai remarqué que quand on été connecté on pouvait aller dans "gérer les sites" et se connecter à un autre site, sauf que pour le moment si je ne me trompe pas lorsqu'on se connecte à un nouveau site alors on coupe la connexion avec le premier site sur lesquel on été connecté, il me semble ? Peut t'on faire en sorte lorsque l'utilisateur fait cette manipulation de proposer 2 choix : soit interrompre la connexion en cours soit de garder la connexion en cours et d'en rouvrir une par dessus ?
Si cela est possible je veux qu'on ajoute un paramètre fonctionnel qui permettra à l'utilisateur de choisir combien de connexion il peut laisser "en attente" en en ouvrant une nouvel (limité à 5 maximum et 1 minimum)
Lorque l'utilisateur choisira de laissé la 1ere connexion ouverte et d'en ouvrir une autre il faudra alors ajouter des onglet (comme sur le web avec une croix en haute à droite de l'onglet), pour le nom de l'onglet il faudra que ca soit soit le nom qui est renseigné dans "gérer les sites" (l'input "nom du site") ou alors si aucun nom n'est renseigné ici il faudra que ca soit le nom de l'hote ATTENTION fait des onglet "assez grand" pour acceuillir une IP de 4 octets
L'endroit exact de l'emplacement de ces onglets doit être entre le "carré" de l'hote et le serveur distant et la barre tout en haut avec les input hote, user,mdp, port, le bouton de déconnexion etc... (regarde l'emplacement de la fleche rouge sur le screen ./_images/issues/issue-17.jpg) il faudra que les onglet apparaisse uniquement que lorsque l'utilisateur fait la manipulation en temps normal l'interface devra resté la meme 
ATTENTION : lorsque 2 connexions sont ouvertes en simultané et que on clique sur le bouton rouge "Déconnecter" il faut un pop-up à l'utilisateur lui spécifiant qu'il à x connexion d'ouverte et que s'il clique sur oui alors il fermera toutes les connexion active sinon il faudra qu'il clique sur annuler

49 = J'ai remarqué que quand on créé une nouvelle connexion en gardant la session en cours cela fonctionne cependant lorsqu'on demande d'ouvrir 2 fois la même connexion -> cela fonctionne aussi ce qui nous ouvre 2 onglet qui sont exactement pareil alors que aucun paramètre de connexion ne change ni le nom renseigné dans glideftp dans les sites enregistrés, ni l'adresse ip (ou nom d'hote), ni l'utilisateur, ni le port ; a mon sens il faudrait faire en sorte de comparer ses information quand l'utilisateur choisi de garder la session en cours et si une informations est differente alors on fait comme maintenant on ouvre un nouvel onglet, en revanche si en comparant les données on voit que toutes les données sont pareil et que l'utilisateur essai de se connecter à la même ressource on déconnecte la session en cours et on reconnecte l'utilisateur à la session qu'il a sélectionnée mais sans nouvel onglet --- regarde dans ./_images/issues/issue-18.jpg (je suis connecté sur ce screen 2 fois à la même ressource avec les mêmes infos)

____

1.7.1

50 = Il faudrait avoir un solution pour pouvoir embarqué toutes les dépendances dans le binaire de Linux -> créé une appimage

51 = Est-ce que la gestion de doublons est effective, je m'explique si un utilisateur transfère le même fichier avec le même nom que ca soit depuis l'hote vers le serveur distant ou l'inverse est-ce que quelque chose est prévu ?
Dans tout les cas il faudrait ajouter un pop-up à l'utilisateur avec les choix suivant :
- Remplacer
- Renommer le fichier sur l'hote
- Renommer le fichier sur le serveur distant
- Annuler

Je te laisse revoir les phrases 

52 = J'ai vu que si je drag and drop depuis l'explorateur de fichier vers le serveur distant dans l'appli
GlideFTP cela ne fonctionne pas, je ne sais pas si cela est possible ?

53 = Il faut supprimer completement le choix de "compte" dans gerer les sites -> Authentification ; il n'a pas lieu d'etre

54 = J'ai vu que sur l'écran principal de connexion on peut selectionner le port voulu qui est par defaut le "21" et que à coter il y a deux petites fleches haut et bas pour monter ou descendre ce chiffre
Mais je suppose que tu as volontairement bloquer le port 22 (car cest le port d'échange de données ftp) ce qui est une bonne chose cependant,
lorsque l'utilisateur clique sur le fleche du haut pour augmenter le chiffre 21 cela ne fonctionne pas car 22 est "bloqué"
Ce qu'il faut faire c'est gardé le port 22 bloqué mais faire en sorte que le fleche du haut fonctionne quand on est au port 21 et qu'on veux incrémenter le port, il faut que le port 22 sois "sauté" et que ca passe tout seul au port 23

55 = Peut-tu faire en sorte que lorsque l'utilisateur clique sur la petite fleche "rafraichir" celle ci tourne 1 fois en cercle
le but est d'avoir une animation "visible" pour l'utilisateur 

