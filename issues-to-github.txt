GlideFTP – Issues to create on GitHub (1 → 55)
================================================
Format: Title / Labels / Description / Solution (closing comment)
================================================

---

#1
Title: [UI] Connection screen layout does not match the design spec
Labels: bug, ui
Version fixed: v1.1
Description:
The connection screen layout is incorrect. The host, user, password, port inputs
and connect button should be centered on the page when no connection is active,
and move to the top bar once connected.
Ref: _images/issues/issue-1.png

Solution (closing comment):
Fixed in v1.1.
App.svelte was restructured with two distinct layout states:
- Disconnected → centered `.connect-card` containing ConnectionBar and the
  link to SiteManager.
- Connected → inputs move to the top bar and the dual file-browser panel is
  shown below.
CSS in style.css updated to use full-height flexbox so each state fills the
viewport correctly.

---

#2
Title: [Bug] "Already connected or connecting" JS error when connecting via Site Manager on fresh launch
Labels: bug
Version fixed: v1.1
Description:
On a fresh app start, opening Site Manager → creating a site → clicking "Connect"
leaves the app stuck on the connection screen with the error:
"already connected or connecting" (wails://wails/).
Ref: _images/issues/issue-2.png / issue-2.1.png

Solution (closing comment):
Fixed in v1.1.
Root cause: manager.Connect() returned an error if a stale connection state was
still set from the previous session.
Fix applied in internal/connection/manager.go: Connect() now unconditionally
disconnects the existing active client before establishing a new one, so the
"already connected" guard can never be hit from the UI.
The connection flow in App.svelte was also adjusted so the state machine always
resets to "disconnected" before a new connect attempt.

---

#3
Title: [UI] Application window does not adapt when maximized or resized
Labels: bug, ui
Version fixed: v1.1
Description:
When the window is maximized or manually enlarged, the UI stays locked at a fixed
maximum size leaving empty space around it.
Ref: _images/issues/issue-3.png

Solution (closing comment):
Fixed in v1.1.
All layout containers in App.svelte and style.css were converted to flexbox with
percentage-based sizing (height: 100%, flex: 1). The html/body/app wrapper chain
was set to height: 100vh so the layout expands to fill the full window regardless
of its size.

---

#4
Title: [UI] Top bar misaligned after connection — some buttons hidden behind the banner
Labels: bug, ui
Version fixed: v1.1
Description:
Once connected the top bar is not correctly aligned. Some action buttons are
partially or fully hidden behind an overlapping banner element.
Ref: _images/issues/issue-4.png

Solution (closing comment):
Fixed in v1.1.
The top bar container in App.svelte was given explicit `flex-shrink: 0` and
correct padding so it never collapses or overlaps the content beneath it.
ConnectionBar.svelte internal layout was also realigned with flexbox so all
buttons remain visible on a single row.

---

#5
Title: [Feature] Add "Default remote directory" field to saved sites
Labels: enhancement
Version fixed: v1.1
Description:
When connecting to a saved site the remote panel always opens at the server root.
Users should be able to configure a default remote directory per site.

Solution (closing comment):
Implemented in v1.1.
- internal/sites/sites.go: added `RemoteDir string` field to the Site struct.
- SiteManager.svelte: new text input bound to form.remoteDir (placeholder "/").
- connection.js connectBySite() and connectBySiteWithPassword(): after a
  successful connect, refreshRemote(site.remoteDir || '/') is called so the
  remote panel opens directly at the configured path.

---

#6
Title: [Feature] Show ".." entry in file browser for parent directory navigation
Labels: enhancement
Version fixed: v1.1
Description:
There is no ".." row in the file browser to navigate to the parent directory.

Solution (closing comment):
Implemented in v1.1 in FileBrowser.svelte.
A ".." row is always rendered at the top of the file list. Click or double-click
calls the onNavigateUp callback. Keyboard: pressing Arrow Up from the first
regular entry sets `parentFocused = true` (the ".." row is highlighted); pressing
Enter while parentFocused calls onNavigateUp. Navigation resets parentFocused via
a reactive statement `$: if (path) parentFocused = false`.

---

#7
Title: [Bug] Drag-and-drop file transfer does not work
Labels: bug
Version fixed: v1.2
Description:
Dragging a file or folder from one panel and dropping it on the other panel does
not trigger a transfer.

Solution (closing comment):
Fixed in v1.2 in FileBrowser.svelte.
Each file row now has `draggable="true"` and sets drag data as JSON:
`{ entries: [{path, name}], fromSide }`. If the dragged row is already part of
the current selection, all selected entries are packed into the entries array.
The drop zone on the opposite panel iterates the full entries array and queues a
transfer for each item. A visual `drag-over` CSS class was added for feedback.

---

#8
Title: [Feature] Resizable splitters for local/remote panels and transfer queue
Labels: enhancement, ui
Version fixed: v1.1
Description:
There is no way to resize the left/right panel split or the transfer queue height.

Solution (closing comment):
Implemented in v1.1.
- App.svelte: a `.browser-splitter` div sits between the two FileBrowser panels.
  mousedown → mousemove computes a percentage (clamped 20–80 %) stored in
  `leftWidth`; both panels use `style="width: {leftWidth}%"` /
  `style="width: {100-leftWidth}%"`.
- TransferQueue.svelte: a resize handle at the top triggers a similar
  mousedown/mousemove loop that adjusts the queue height in pixels.

---

#9
Title: [Bug] File lists do not auto-refresh after a transfer completes
Labels: bug
Version fixed: v1.1
Description:
After a transfer finishes, the file panels are not refreshed automatically.

Solution (closing comment):
Fixed in v1.1.
- internal/transfer/queue.go: at job completion, emits the Wails event
  `transfer:progress` with the final status, and the job data is exposed via
  the `completedTransfer` mechanism.
- frontend/src/stores/transfers.js: exports a `completedTransfer` writable store;
  the EventsOn('transfer:progress') handler sets it to `{ ...job, _ts: Date.now() }`
  on each completion (the _ts ensures the reactive trigger fires even if the same
  job ID completes twice).
- App.svelte: `$: if ($completedTransfer) { refreshLocal(); refreshRemote(); }`
  re-lists both panels automatically after every finished transfer.

---

#10
Title: [Feature] Right-click context menu in file browser (new folder, transfer, rename, delete)
Labels: enhancement
Version fixed: v1.2
Description:
No context menu is available on right-click in the file browser panels.

Solution (closing comment):
Implemented in v1.2 in FileBrowser.svelte.
An `on:contextmenu` handler on file rows and on the panel background builds a
positioned menu:
- Right-click on a file/folder → Rename / Transfer / Delete (deletes the full
  selection if the right-clicked item is already selected).
- Right-click on empty area → New Folder.
The menu is dismissed on the next click via a svelte:window on:click handler.

---

#11
Title: [Feature] Column sorting in file browser (name, size, date)
Labels: enhancement
Version fixed: v1.1
Description:
Files and folders cannot be sorted; the listing order is whatever the server
or OS returns.

Solution (closing comment):
Implemented in v1.1 in FileBrowser.svelte.
Each column header (Name, Size, Date) is a clickable button. State variables
`sortKey` and `sortDir` ('asc'/'desc') drive a reactive sort on the entries
array. A second click on the same header reverses the order. Directories are
always grouped before files regardless of the active sort key.

---

#12
Title: [Bug] Multi-file selection not working (Shift+click, Ctrl+click, rubber-band)
Labels: bug
Version fixed: v1.1
Description:
Only single-file selection works. Shift+click, Ctrl+click, and click-drag
rubber-band selection are all broken.

Solution (closing comment):
Fixed in v1.1 in FileBrowser.svelte.
- Ctrl+click: toggles the clicked entry in the `selected` set.
- Shift+click: selects all entries between the last clicked index and the
  current one (range selection).
- Rubber-band: mousedown on empty area starts a drag-select state; mousemove
  updates a selection rectangle drawn as an SVG overlay; mouseup finalizes the
  selection by checking which rows intersect the rectangle.

---

#13
Title: [Bug] F2 shortcut does not trigger rename on the selected file/folder
Labels: bug
Version fixed: v1.1
Description:
Pressing F2 on a selected entry does not open the rename input.

Solution (closing comment):
Fixed in v1.1 in FileBrowser.svelte.
The panel wrapper div was given `tabindex="-1"` so it can receive keyboard focus.
A click on any file row calls `panelEl.focus()`. The panel's `on:keydown` handler
checks `e.key === 'F2'` and triggers the rename inline input for the focused
entry. Escape cancels; Enter confirms and calls onRename.

---

#14
Title: [Bug] Settings panel inaccessible while connected; "Show hidden files" toggle broken
Labels: bug
Version fixed: v1.1
Description:
Once connected the settings gear button does nothing. The "Show hidden files"
toggle in settings has no effect.

Solution (closing comment):
Fixed in v1.1.
- App.svelte: the settings panel visibility (`showSettings`) was incorrectly
  gated on the connection state. The gate was removed so the panel can always
  be opened.
- SettingsPanel.svelte: all on/off toggles were rewritten as
  `<button class="sw" class:on={val} on:click={() => toggle(key)}>` — WebKit-GTK
  does not fire click events on hidden checkboxes (see project patterns). Custom
  `−`/`+` buttons were added for number inputs for the same reason.
- handleSettingsSaved() in App.svelte calls refreshLocal() + refreshRemote() so
  showHiddenFiles changes take effect immediately without reconnecting.

---

#15
Title: [Feature] Allow manual path entry in the path bar of each panel
Labels: enhancement
Version fixed: v1.1
Description:
The path bar at the top of each panel is read-only; users cannot type a path
manually.

Solution (closing comment):
Implemented in v1.1 in FileBrowser.svelte.
Clicking the path display switches it to an `<input type="text">` bound to an
`editPath` variable. Pressing Enter calls navigate(editPath); pressing Escape
reverts to display mode without navigating. The input auto-focuses via
`use:focus` on render.

---

#16
Title: [UI] Top bar slightly clipped after connection — disconnect button partially hidden
Labels: bug, ui
Version fixed: v1.1
Description:
The top bar is visually cut off on one side; the red disconnect button is
partially hidden behind the edge of the bar.
Ref: _images/issues/issue-5.png

Solution (closing comment):
Fixed in v1.1.
The `.topbar` container in App.svelte was given `overflow: visible` and correct
horizontal padding so no child element is clipped. The disconnect button in
ConnectionBar.svelte was also given `flex-shrink: 0` to prevent it from being
squeezed off-screen on narrow windows.

---

#17
Title: [Feature] Allow removing individual completed transfers from the queue
Labels: enhancement
Version fixed: v1.1
Description:
The "Completed" tab only has a global "Clear all" button; individual entries
cannot be removed one by one.

Solution (closing comment):
Implemented in v1.1.
- internal/transfer/queue.go: RemoveJob(id) removes a job by ID and emits the
  Wails event `transfer:removed`.
- app.go: RemoveTransfer(id) is the JS-callable wrapper.
- frontend/src/stores/transfers.js: removeTransfer(id) calls the binding;
  the EventsOn('transfer:removed') handler filters the local store.
- TransferQueue.svelte: each completed row now has a ✕ button that calls
  removeTransfer(job.id).

---

#18
Title: [Bug] Most settings are non-functional (port spinners, default local dir, all toggles)
Labels: bug
Version fixed: v1.1
Description:
Only language and dark/light theme work. Port arrows do nothing, default local
directory is ignored on reconnect, all on/off toggle buttons have no effect.

Solution (closing comment):
Fixed in v1.1 — complete rework of SettingsPanel.svelte and related stores.
- Toggle buttons: replaced hidden-checkbox pattern with WebKit-GTK compatible
  `<button class="sw" class:on={val}>` (native checkbox click events are broken
  under WebKit-GTK when the input is hidden).
- Number steppers: replaced native `<input type="number">` spinners (invisible
  under WebKit-GTK) with custom `−`/`+` buttons calling a step(key, delta, min, max)
  helper. Native spinners are hidden via `-webkit-appearance: none`.
- Default local dir: initLocalDir(startDir) in connection.js now receives the
  saved defaultLocalDir from loadSettings() at startup (passed from App.svelte).
- All settings are persisted via settings.js saveSettings() and re-read on load.

---

#19
Title: [Feature] Path bar autocomplete when typing a directory path
Labels: enhancement
Version fixed: v1.2
Description:
When typing manually in the path bar there are no suggestions for matching
subdirectories.

Solution (closing comment):
Implemented in v1.2 in FileBrowser.svelte.
While the path bar is in edit mode, an `on:input` handler debounces (150 ms)
then calls LocalListDir or RemoteListDir on the parent of the currently typed
path, filters the returned entries to directories whose names start with the
last segment typed, and renders a dropdown below the input. Clicking a suggestion
completes the path and navigates immediately.

---

#20
Title: [Feature] Add a freeform "Note" field to saved sites
Labels: enhancement
Version fixed: v1.2
Description:
Users have no way to attach notes (credentials reminder, server description,
etc.) to a saved site.
Ref: _images/issues/issue-6.png

Solution (closing comment):
Implemented in v1.2.
- internal/sites/sites.go: `Note string` field added to the Site struct.
- SiteManager.svelte form: `<textarea class="note-area" bind:value={form.note}>`
  added below the other fields.
- Site detail view: note text is displayed in a styled block with a left border.
  A copy-to-clipboard button was added alongside it (see also issue #43).

---

#21
Title: [Bug] Delete key does not delete selected files; confirmation popup not implemented
Labels: bug
Version fixed: v1.2
Description:
Pressing Delete on a selected file/folder does nothing. The "Confirm before
delete" setting exists in the UI but the popup is never shown.

Solution (closing comment):
Fixed in v1.2 in FileBrowser.svelte.
- The panel's keydown handler now calls handleDelete(selected) on the Delete key.
- handleDelete populates a `confirmDeleteEntries` array. If the ConfirmOnDelete
  setting is on, a popup is shown listing the file name (single item) or
  "N elements" (multiple). The user confirms or cancels.
- doDeleteAll() iterates the full array, calls onDelete for each entry, then
  triggers a single panel refresh at the end.
- The right-click context menu "Delete" option follows the same flow.

---

#22
Title: [Feature] Add transfer speed limit setting (MB/s)
Labels: enhancement
Version fixed: v1.2
Description:
Users on slow or metered connections cannot cap the upload/download speed.

Solution (closing comment):
Implemented in v1.2.
- internal/settings/settings.go: MaxTransferSpeedKBps int field added (0 = unlimited).
- internal/transfer/queue.go: Queue.SetSpeedLimit(kbps int) stores speedLimitBps.
  The progressReader and progressWriter check the elapsed time after each chunk
  and sleep to enforce the cap.
- app.go: queue.SetSpeedLimit(appSettings.MaxTransferSpeedKBps) called on startup
  and on every settings save.
- SettingsPanel.svelte: number input (with custom −/+ steppers) for the speed
  limit value, labeled in KB/s (0 = no limit).

---

#23
Title: [Bug] "Ask for password" auth type still persists the password to disk
Labels: bug
Version fixed: v1.2
Description:
When creating a site with "Ask for password" auth, the password entered during
setup is saved to disk. It must never be stored for this auth type.

Solution (closing comment):
Fixed in v1.2 in SiteManager.svelte.
saveSite() now clears the password field before persisting when
`toSave.authType === 'ask_password'`:
  `if (toSave.authType === 'ask_password') toSave.password = '';`
At connect time, if the site has ask_password auth, a modal password prompt is
shown (showPasswordPrompt state). The entered password is passed directly to
app.ConnectWithPassword() and never written to the site file.

---

#24
Title: [Feature] Export and import saved sites as JSON
Labels: enhancement
Version fixed: v1.2
Description:
Users cannot back up their saved sites or transfer them to another machine.

Solution (closing comment):
Implemented in v1.2.
- app.go: ExportSites() opens a Wails save-file dialog, marshals all sites to
  indented JSON, and writes the file. ImportSites() opens an open-file dialog,
  reads and parses the JSON, merges entries (skipping duplicates by ID), and
  returns the count of imported sites.
- SiteManager.svelte: "Export" and "Import" buttons added to the modal header.
  After a successful import, loadSites() is called and an alert shows the count.

---

#25
Title: [Feature] Accent color picker (RGB / HEX / HSV) in Appearance settings
Labels: enhancement, ui
Version fixed: v1.2
Description:
The blue accent color is hardcoded. Users cannot customise it.
Ref: _images/issues/issue-rgb.png

Solution (closing comment):
Implemented in v1.2.
- frontend/src/components/ColorPicker.svelte created: HSV canvas (mousedown drag
  moves the saturation/value cursor), hue slider, editable RGB and HEX inputs
  that stay in sync, "Default" button restoring #5B8AF5, and an 8-color history
  strip (see also issue #38). The component opens as a z-500 overlay on top of
  the settings panel.
- frontend/src/stores/settings.js: applyAccentColor(hex) sets --accent,
  --accent-hover, and --accent-subtle CSS custom properties on
  document.documentElement. Called on startup and on every save.
- internal/settings/settings.go: AccentColor string field persisted to
  ~/.config/GlideFTP/settings.json.

---

#26
Title: [Bug] Rubber-band (click-and-drag) multi-selection does not work
Labels: bug
Version fixed: v1.2
Description:
Holding the left mouse button and dragging over the file list does not draw a
selection rectangle or select any files.
(Related to #12.)

Solution (closing comment):
Fixed in v1.2 in FileBrowser.svelte.
mousedown on the empty panel background (not on a file row) sets a rubber-band
start coordinate. mousemove updates rubberRect (top, left, width, height) drawn
as an SVG `<rect>` overlay at pointer-events: none. mouseup computes which row
DOM rects intersect rubberRect via getBoundingClientRect() and adds them to the
selected set. The overlay is cleared after mouseup.

---

#27
Title: [Bug] Clicking ".." on remote panel freezes the UI / drops the connection
Labels: bug, critical
Version fixed: v1.2
Description:
After navigating via ".." on the remote panel the UI becomes unresponsive:
clicks on files have no effect. Disconnecting and reconnecting restores it.
Auto-reconnect does not help.

Solution (closing comment):
Fixed in v1.2 in internal/connection/manager.go.
manager.ListDir() now wraps the blocking client.List() call in a goroutine and
uses a select with time.After(30 s) as a timeout. If the listing doesn't return
in time, the manager calls Disconnect() on the active client and returns a
descriptive error. The UI in App.svelte / FileBrowser.svelte catches this error,
sets connectionStatus to 'disconnected', and clears the remote panel — instead
of hanging indefinitely.

---

#28
Title: [Bug] Multi-file transfer broken — drag-and-drop sends only 1 file; transfer button fails all
Labels: bug, critical
Version fixed: v1.2
Description:
With multiple files selected:
- Drag-and-drop only transfers the first file; the rest are silently ignored.
- The "Transfer" button queues all files but all fail. Retrying individually works.

Solution (closing comment):
Fixed in v1.2.
Two separate root causes:
1. Drag-and-drop (FileBrowser.svelte): the dragstart handler now checks if the
   dragged row is part of the current selection; if so it packs ALL selected
   entries into the drag data JSON array. The drop handler iterates the full
   array and queues a transfer per entry.
2. Concurrent FTP transfers (internal/connection/ftp.go): FTPClient gained a
   sync.Mutex protecting every method. The jlaffaye/ftp library is not
   thread-safe; without the mutex, concurrent queue workers corrupt the control
   connection, causing all parallel jobs to fail. With the mutex, each job
   acquires the lock for its operation in sequence.

---

#29
Title: [Feature] Password visibility toggle (eye icon) in the "Password required" prompt
Labels: enhancement, ui
Version fixed: v1.2
Description:
The password prompt when connecting to an "ask_password" site has no way to
reveal the typed password.

Solution (closing comment):
Implemented in v1.2 in SiteManager.svelte.
A `showPwd` boolean drives two separate `<input>` blocks inside an `{#if}/{:else}`
— one type="text", one type="password", both bound to the same `promptPassword`
variable. (Svelte 3 does not allow a dynamic `type` attribute on a two-way-bound
input under WebKit-GTK.) An eye SVG button toggles showPwd. The icon switches
between "open eye" and "crossed eye" variants.

---

#30
Title: [Bug] Cancel button in transfer queue has no effect
Labels: bug
Version fixed: v1.2
Description:
Clicking "Cancel" on a pending or in-progress transfer in the queue does nothing.
Ref: _images/issues/issue-7.png

Solution (closing comment):
Fixed in v1.2 in internal/transfer/queue.go.
Each Job now holds a `cancelFn context.CancelFunc` that is set when queue.run()
starts the job's goroutine. progressReader.Read() and progressWriter.Write() both
call `ctx.Err()` before processing each chunk and return an error if the context
is cancelled, cleanly aborting the in-progress I/O. The exported Cancel(id)
method handles both cases: pending jobs are removed from the queue directly;
running jobs have their cancelFn() called.

---

#31
Title: [Feature] Auto-couple SFTP protocol ↔ Interactive / Key auth type in Site Manager
Labels: enhancement
Version fixed: v1.2
Description:
Selecting SFTP doesn't automatically set the right auth type and vice-versa,
making the SSH key input hard to discover.

Solution (closing comment):
Implemented in v1.2 in SiteManager.svelte.
Two helper functions were added:
- setProtocol(p): if p === 'sftp' and authType is not already 'interactive' or
  'key', switches authType to 'interactive' and port to 22. If p === 'ftp',
  resets to 'normal' auth and port 21.
- setAuthType(a): if a is 'interactive' or 'key', switches protocol to 'sftp'
  and port to 22. Otherwise switches to 'ftp' and port 21.
Both functions update form immutably with a spread. The SSH key file input is
shown with `{#if form.protocol === 'sftp' && form.authType === 'key'}`.

---

#32
Title: [Bug] Deleting multiple selected files only deletes one
Labels: bug
Version fixed: v1.3
Description:
When multiple files are selected and deletion is confirmed, only the first item
in the selection is actually deleted.

Solution (closing comment):
Fixed in v1.3 in FileBrowser.svelte.
doDeleteAll() was calling onDelete once with the full array, but the backend
expected individual calls. The function now iterates the full confirmDeleteEntries
array, calls onDelete(entry) for each item sequentially, and triggers a single
refreshLocal/refreshRemote at the very end to avoid one listing call per file.

---

#33
Title: [Bug] Arrow keys and Enter do not navigate the file browser
Labels: bug
Version fixed: v1.4
Description:
Arrow Up/Down don't move the selection; Enter doesn't open a directory. Keyboard
navigation is completely absent.

Solution (closing comment):
Fixed in v1.4 in FileBrowser.svelte.
The panel div (tabindex="-1") keydown handler was extended:
- ArrowDown: increments focusedIndex (clamped to entries length), scrolls the
  row into view.
- ArrowUp: decrements focusedIndex; when at index 0 it sets parentFocused=true
  which highlights the ".." row.
- Enter on a directory entry: calls the navigate callback.
- Enter while parentFocused: calls onNavigateUp.
A `$: if (path) parentFocused = false` reactive resets focus state on navigation.

---

#34
Title: [UI] Host input on connection screen too narrow for a full IPv4 address
Labels: bug, ui
Version fixed: v1.4
Description:
The host input field is too short — a full four-octet IPv4 like 192.168.124.155
is visually truncated.

Solution (closing comment):
Fixed in v1.4 in ConnectionBar.svelte.
The host input min-width was increased (from ~120 px to 200 px) and the overall
connection bar container was given a slightly larger max-width so the port field
and connect button remain on the same row (see also issue #40).

---

#35
Title: [Feature] Right-click → Paste in the "Password required" prompt
Labels: enhancement
Version fixed: v1.4
Description:
WebKit-GTK suppresses the native browser context menu inside the WebView. The
password input in the connection prompt has no paste option.

Solution (closing comment):
Implemented in v1.4 in SiteManager.svelte.
`on:contextmenu={handlePwdContextMenu}` on both password inputs (show/hide
variants) records the click coordinates in `pasteMenu`. A positioned div is
rendered at those coordinates with a single "Paste" button. The handler calls
`navigator.clipboard.readText()` and appends the result to promptPassword.
A `<svelte:window on:click={() => pasteMenu = null}>` dismisses the menu on
any outside click.

---

#36
Title: [Bug] Top bar inputs show placeholder values instead of the active connection values
Labels: bug
Version fixed: v1.4
Description:
Once connected, the host, user, and port inputs in the top bar still show their
empty default values instead of the real connection parameters.

Solution (closing comment):
Fixed in v1.4.
- frontend/src/stores/connection.js: activeConnectionConfig writable store
  (initially null). Set to { protocol, host, port, user } on every successful
  connect (connectBySite, connectBySiteWithPassword, connect, addConnection).
  Cleared to null on disconnect.
- ConnectionBar.svelte: inputs read from activeConnectionConfig when it is not
  null, falling back to the user-typed values otherwise. The password field
  always displays the string "············" (12 dots) when connected, regardless
  of the actual password.

---

#37
Title: [Feature] Graceful disconnect when the application window is closed or interrupted
Labels: enhancement
Version fixed: v1.4
Description:
Closing the window while connected leaves the session open server-side.

Solution (closing comment):
Implemented in v1.4.
main.go registers an OnShutdown hook with the Wails runtime:
  `wails.OnShutdown(ctx, func() { app.shutdown(ctx) })`
app.shutdown() in app.go calls connMgr.Disconnect() which closes the active FTP
or SFTP connection cleanly. This ensures the server receives a proper BYE/QUIT
or SSH channel close regardless of how the window is dismissed.

---

#38
Title: [Feature] Accent color history (last 8 colors) in the color picker
Labels: enhancement
Version fixed: v1.4
Description:
The color picker has no memory of previously used colors.

Solution (closing comment):
Implemented in v1.4 in ColorPicker.svelte.
On each color application, the hex value is prepended to an array stored in
localStorage under the key `glideftp_color_history`. Duplicates are removed and
the array is trimmed to 8 entries before saving. On component mount the history
is loaded and rendered as clickable circular swatches above the footer row.
Clicking a swatch instantly populates all color inputs and applies the color.

---

#39
Title: [Bug] Download from remote server stays at 0% and never completes
Labels: bug, critical
Version fixed: v1.5
Description:
Uploading from local → remote works. Downloading remote → local shows 0% progress
and no bytes are ever written.
Ref: _images/issues/issue-8.png

Solution (closing comment):
Fixed in v1.5 in internal/connection/ftp.go.
Root cause: FileSize() was called AFTER Retr() had already opened the data
connection. On Synology NAS (and some other FTP servers) issuing a SIZE command
on the control connection while a data transfer is in flight violates the FTP
state machine; the server replies with 0, causing the progress tracking to
believe nothing was transferred and the download was empty.
Fix: moved the FileSize() call to before Retr() so the size is known before the
data connection opens. The transfer then proceeds and completes normally.

---

#40
Title: [UI] Port field and connect button wrap to a second line after host input was widened
Labels: bug, ui
Version fixed: v1.5
Description:
After widening the host input (issue #34), the port field and connect button now
overflow to a second row.
Ref: _images/issues/issue-9.png

Solution (closing comment):
Fixed in v1.5 in ConnectionBar.svelte.
The `.topbar-form` container CSS was adjusted (increased max-width / min-width,
`flex-wrap: nowrap`, `overflow: visible`) so the host input, port field, and
connect button all stay on a single line even with the larger host input.

---

#41
Title: [Feature] Cross-compile Windows .exe and restructure build output paths
Labels: enhancement, build
Version fixed: v1.6
Description:
No Windows executable exists. Build output has no clear platform separation.
Expected: build/bin/linux/GlideFTP and build/bin/windows/GlideFTP.exe.
Ref: _images/issues/issue-10.jpg

Solution (closing comment):
Implemented in v1.6.
build.sh created at project root with three modes:
  ./build.sh            → builds both Linux and Windows
  ./build.sh linux      → wails build -tags webkit2_41; mv → build/bin/linux/GlideFTP
  ./build.sh windows    → CC=x86_64-w64-mingw32-gcc CGO_ENABLED=1 GOOS=windows
                          wails build -platform windows/amd64;
                          mv → build/bin/windows/GlideFTP.exe
Requires mingw-w64-gcc for the Windows target (sudo pacman -S mingw-w64-gcc).
Windows builds must NOT use -tags webkit2_41 (uses WebView2 instead).
README.md updated with full build instructions.

---

#42
Title: [Feature] Show real-time transfer speed (KB/s or MB/s) in the transfer queue
Labels: enhancement
Version fixed: v1.6
Description:
While a transfer is in progress the queue entry shows size and bytes done but
no speed indicator.
Ref: _images/issues/issue-11.jpg

Solution (closing comment):
Implemented in v1.6 in TransferQueue.svelte.
A `speeds` Map (id → bytes/sec) tracks per-transfer speed. On each
`transfer:progress` event the handler computes the delta between the new
bytesDone and the last recorded bytesDone, divided by the elapsed time. Updates
below a 250 ms window are skipped to avoid noise from tiny deltas. The speed is
displayed next to the progress label in the accent color, formatted as
"X.X KB/s" below 1 MB/s or "X.X MB/s" above.

---

#43
Title: [Feature] Copy-to-clipboard button next to site notes in Site Manager
Labels: enhancement, ui
Version fixed: v1.6
Description:
When viewing a saved site with a note there is no quick way to copy the text.
Ref: _images/issues/issue-12.jpg

Solution (closing comment):
Implemented in v1.6 in SiteManager.svelte.
The note display block was wrapped in a `.view-note-wrap` relative container.
A `.copy-note-btn` (clipboard SVG icon, position: absolute right: 8px) is
overlaid on the note. copyNote(text) calls navigator.clipboard.writeText(text)
and sets `noteCopied = true` for 1500 ms, swapping the clipboard icon for a
checkmark tick to give visual confirmation.

---

#44
Title: [UI] Copy button on site note is misaligned / not centered
Labels: bug, ui
Version fixed: v1.6
Description:
The copy-to-clipboard button added in issue #43 is visually offset and not
vertically centred relative to the note block.
Ref: _images/issues/issue-13.jpg

Solution (closing comment):
Fixed in v1.6 in SiteManager.svelte CSS.
.copy-note-btn:
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  right: 8px;
.view-note-wrap:
  position: relative;
  display: inline-flex;
  width: 100%;
.view-note:
  flex: 1;
  padding-right: 32px;   ← prevents note text from sliding under the button

---

#45
Title: [Bug] Ctrl+Z (undo) does not work in text inputs
Labels: bug
Version fixed: v1.6
Description:
Pressing Ctrl+Z inside any text input does not undo the last typed character.
WebKit-GTK does not fire native undo events in Svelte-bound inputs.

Solution (closing comment):
Fixed in v1.6 in App.svelte.
A global `handleKeydown` listener is attached via `<svelte:window on:keydown>`.
When `e.ctrlKey && e.key === 'z'` is detected and the active element is an
INPUT or TEXTAREA:
1. `e.preventDefault()` stops any default (no-op) handling.
2. `document.execCommand('undo')` triggers the WebKit undo stack.
3. A synthetic `new Event('input', { bubbles: true })` is dispatched on the
   target so Svelte re-reads the DOM value and syncs its reactive variable.

---

#46
Title: [Bug] SFTP connection fails when using a PuTTY .ppk private key file
Labels: bug
Version fixed: v1.6
Description:
Connecting via SFTP with a .ppk key file results in a parse error. Only OpenSSH
PEM format is handled by the standard Go crypto/ssh library.
Ref: _images/issues/issue-14.jpg / issue-15.jpg

Solution (closing comment):
Fixed in v1.6.
internal/connection/ppk.go created (243 lines):
- isPPKFile(path): reads the first line to detect "PuTTY-User-Key-File-2:" or
  "-3:" headers.
- parsePPKFile(path): full parser for PPK v2 and v3 format, supporting RSA and
  Ed25519 key types. Decodes the base64 public and private blobs, reconstructs
  the ssh.Signer directly without spawning puttygen.
  Encrypted PPK keys return a descriptive error asking the user to convert with
  PuTTYgen.
internal/connection/sftp.go: loadPrivateKey(path, passphrase) now calls
isPPKFile() first; if true it routes to parsePPKFile() instead of
ssh.ParsePrivateKey(). Detection is fully automatic — no UI change needed.

---

#47
Title: [Bug] Application cannot be maximized or exceed a fixed window size on large screens
Labels: bug, ui
Version fixed: v1.7
Description:
On a 3440×1440 monitor the window cannot be maximized or resized beyond a fixed
limit, leaving large empty margins.
Note: behaviour confirmed as still not fixed on Hyprland (Wayland).
Ref: _images/issues/issue-16.jpg

Solution (closing comment):
Partially fixed in v1.7.
wails.json: MaxWidth and MaxHeight were set to 0 (unlimited), removing the
hardcoded size cap. The window can now be freely resized and maximized on X11.
Known limitation: under Hyprland (Wayland compositor) the window still does not
maximise correctly — this appears to be a Wails v2 / WebKit-GTK Wayland
compositor interaction issue outside the scope of GlideFTP itself. Leaving open
pending an upstream Wails fix.

---

#48
Title: [Feature] Multi-connection support with tab management and keep-or-replace prompt
Labels: enhancement
Version fixed: v1.7
Description:
When connecting to a new site while already connected, the current connection is
always silently replaced. Users should be offered the choice to keep the existing
connection open and open the new one alongside it in a separate tab.
Ref: _images/issues/issue-17.jpg

Solution (closing comment):
Implemented in v1.7 — significant work across backend and frontend.

Backend (internal/connection/manager.go):
- Connection map (id → Client) replaces the single-client model; protected by
  sync.RWMutex.
- ConnectNew(cfg): opens a new connection without closing existing ones; the new
  connection becomes active.
- CloseOne(id): closes a specific connection; if it was active, switches to the
  most-recently-added remaining one.
- SwitchTo(id): makes a connection active without reconnecting.

Go bindings (app.go):
- ConnectToSiteAdditional(siteID, overridePassword): adds a connection alongside
  existing ones.
- GetConnections(): returns []ConnInfo for all open connections.
- SwitchConnection(id) / CloseConnection(id) / GetActiveConnectionID().

Frontend stores (connection.js):
- connections writable store: array of {id, name, host, protocol, port, user,
  remotePath}.
- activeConnectionId writable store.
- addConnection(), switchTab() (saves current path before switching),
  closeTab() (auto-activates last remaining tab if the closed one was active).

UI (App.svelte):
- .conn-tabs strip appears between the top bar and file panels, but only when
  $connections.length > 1. Each tab shows the site name (or host), has an accent
  underline when active, and a ✕ button to close it.
- Disconnect-all overlay: when the disconnect button is clicked with 2+ open
  connections, a confirmation popup explains how many connections will be closed.

Site Manager (SiteManager.svelte):
- keep-or-replace dialog: when already connected and "Connect" is clicked, the
  user is offered "Keep current and open new" / "Replace current connection" /
  "Cancel".
- canAddConnection guard checks $connections.length < maxConnections before
  enabling the "Keep" button.

Settings:
- internal/settings/settings.go: MaxConnections int field (default 3, range 1–5).
- SettingsPanel.svelte: MaxConnections exposed with custom −/+ stepper buttons.

---

#49
Title: [Bug] Connecting to the same site twice opens a duplicate tab with identical parameters
Labels: bug
Version fixed: v1.7 (corr)
Description:
When already connected to a site and choosing "Keep current session and open new"
from the keep-or-replace dialog, selecting the exact same site a second time
opens a second identical tab — same host, port, protocol, and user. No visual
distinction exists between the two tabs and there is no way to tell them apart.
Ref: _images/issues/issue-18.jpg

Solution (closing comment):
Fixed in v1.7 (corr) in SiteManager.svelte — doKeepAndAdd().
Before calling addConnection(), the function now checks whether any entry in
$connections already has the same host / port / protocol / user as the site
being connected to:

  const isDuplicate = $connections.some(c =>
    c.host === site?.host &&
    Number(c.port) === Number(site?.port) &&
    c.protocol === site?.protocol &&
    c.user === site?.user
  );

Two cases:
- Duplicate detected, normal auth: connectBySite() is called instead of
  addConnection() — the active tab is refreshed/reconnected without a new tab
  being created.
- Duplicate detected, ask_password auth: promptIsAdd is set to false so the
  password prompt routes to connectBySiteWithPassword() (reconnect) instead of
  addConnection() (new tab).
- No duplicate: original behaviour unchanged — addConnection() opens a new tab.

The site name field is intentionally NOT compared since two sites can have
different names but point to the same server endpoint.

---

1.7.1

#50
Title: [Build] Linux binary requires libwebkit2gtk-4.1 on the target system — add AppImage distribution
Labels: bug, build, linux
Version fixed: v1.7 (corr)
Description:
Running the Linux binary on a system where libwebkit2gtk-4.1 is not installed
fails immediately with:
  ./GlideFTP: error while loading shared libraries: libwebkit2gtk-4.1.so.0:
              cannot open shared object file: No such file or directory
The Windows binary is unaffected because it uses WebView2 (built into Windows
10/11). The error appears on any Linux distro that does not ship webkit2gtk-4.1
by default (e.g. Ubuntu 20.04, or systems where only webkit2gtk-4.0 is present).

Root cause: Wails v2 on Linux uses WebKit2GTK as a dynamically linked system
library — it cannot be statically compiled into the binary. Building with
-tags webkit2_41 links against libwebkit2gtk-4.1 specifically. WebKit2GTK is
always a runtime dependency; the binary alone is not self-contained on Linux.

Solution (closing comment):
Fixed in v1.7 (corr) by adding AppImage support to build.sh.

build.sh extended with a new build_appimage() function and a new target:
  ./build.sh appimage   → builds AppImage only (builds Linux binary first if missing)
  ./build.sh            → now produces all three: Linux binary + Windows exe + AppImage

The function downloads linuxdeploy and linuxdeploy-plugin-appimage (cached in
tools/, gitignored) on first run, then packages the binary with all its shared
library dependencies (including libwebkit2gtk-4.1 and the full GTK3 stack) into
a self-contained build/bin/linux/GlideFTP-x86_64.AppImage. Users on any Linux
distro can run it directly with no prior installation — chmod +x and execute.

APPIMAGE_EXTRACT_AND_RUN=1 is set so linuxdeploy itself does not require FUSE.
tools/ added to .gitignore (downloaded AppImage tools, not source).

---

#51
Title: [Feature] Duplicate file detection with conflict resolution popup when transferring
Labels: feature, ui
Version fixed: v1.7.1
Description:
When transferring a file whose name already exists in the destination folder,
the app overwrites it silently. There is no way to know a conflict occurred or
choose what to do.

The expected behaviour is a popup dialog with four choices:
  - Replace — overwrite the existing file
  - Rename on host — save the transferred file with an auto-generated name
    locally (e.g. "file (1).txt")
  - Rename on server — save the transferred file with an auto-generated name
    on the server (e.g. "file (1).txt")
  - Cancel — skip the conflicting file

For multi-file transfers, all conflicting files are grouped in one popup.
Non-conflicting files in the same selection are queued immediately.

Solution (closing comment):
Fixed in v1.7.1.

FileBrowser.svelte receives a new prop `otherEntries` (the entry list of the
opposite panel, passed from App.svelte). When a transfer is requested via the
toolbar button or the context menu:

1. `checkConflicts(entries)` compares source names against `otherEntries` names
   (case-insensitive), splitting the selection into `conflicts` / `nonConflicts`.
2. Non-conflicting files are queued immediately via `doQueueTransfer()`.
3. If any conflicts exist, `conflictPending = { conflicts }` triggers the popup.

The popup shows the filename (single conflict) or "N items" (multiple), with
four buttons:
  - Replace → `doQueueTransfer(entry)` (same dest path, overwrites)
  - Rename on host / Rename on server → `doQueueTransfer(entry, getAutoName(entry.name))`
    Both call `getAutoName()` which increments `(1)`, `(2)`... until the generated
    name does not exist in `otherEntries`. Both options rename the destination.
  - Cancel (Esc / click outside) → `conflictPending = null`

Drag-and-drop transfers bypass conflict detection intentionally (inline with
typical FTP client behaviour — overwrite silently on DnD).

App.svelte: `otherEntries={$remoteEntries}` added to local FileBrowser;
`otherEntries={$localEntries}` added to remote FileBrowser.

---

#52
Title: [Feature] OS drag-and-drop from system file manager into the app
Labels: feature, wont-fix
Version fixed: —
Description:
Dragging files from the system file explorer (Nautilus, Thunar, Windows
Explorer, etc.) and dropping them into the GlideFTP remote panel does not
trigger a transfer. The drop is silently ignored.

Investigation:
Wails v2 on Linux embeds the UI in a WebKit-GTK WebView. The WebKit-GTK context
does not expose native OS-level drag-and-drop events (dragged file paths) to the
JavaScript layer. The `dataTransfer` object in `dragover`/`drop` events carries
no files or `text/uri-list` data when the drag originates from outside the
WebView — it arrives empty.

On Windows (WebView2) the situation may differ, but Wails v2 does not provide
any cross-platform API to bridge OS DnD into the frontend.

Workaround: use the transfer queue (select files in the local panel and click
the transfer arrow), or drag items between the two panels inside the app.

Solution (closing comment):
Not implemented — technically not feasible with Wails v2 + WebKit-GTK on Linux.
The WebView does not receive file paths from OS-level drag sources.
Marked as wont-fix. A future Wails v3 or native GTK integration may unlock this.

---

#53
Title: [UI] Remove "Account" authentication type from Site Manager
Labels: bug, ui
Version fixed: v1.7.1
Description:
The Site Manager authentication dropdown contains an "Account" option
(`value: 'account'`) that maps to a non-standard FTP AUTH type not supported
by the application backend. Selecting it has no useful effect and may confuse
users.

Solution (closing comment):
Fixed in v1.7.1.

Removed `{ value: 'account', label: t('authAccount') }` from the `authTypes`
array in SiteManager.svelte.
Removed the `authAccount` key from both i18n files (en.js and fr.js).
The remaining auth types are: Normal, Anonymous, Ask password, Interactive,
SSH Key.

---

#54
Title: [Bug] Port increment from 21 blocked — clicking "up" arrow does nothing when FTP port is 21
Labels: bug, ui
Version fixed: v1.7.1
Description:
On the main connection bar, the port field has up/down stepper arrows. Port 22
is intentionally blocked for FTP (a reactive statement resets it to 21). This
causes the "up" arrow to appear to do nothing when the port is 21: it tries to
go to 22, the reactive guard resets it back to 21, and the user sees no change.

Solution (closing comment):
Fixed in v1.7.1.

The native `<input type="number">` port field was replaced with a custom stepper
(two `<button>` elements flanking a number input) following the established
WebKit-GTK pattern used in SettingsPanel.

A `stepPort(delta)` function handles increment/decrement:
  - Computes `next = port + delta`
  - If `protocol === 'ftp'` and `next === 22`, skips to `next + delta` (23 going
    up, 21 going down — but 21 going down from 22 would be unusual so the bound
    clamp handles it)
  - Clamps to [1, 65535]

The existing reactive guard `$: if (!isConnected && protocol === 'ftp' && port === 22) port = 21`
remains in place to handle direct keyboard input of 22.

---

#55
Title: [UI] Refresh button should animate (one full rotation) on click
Labels: feature, ui
Version fixed: v1.7.1
Description:
Clicking the refresh button (↻) in either file browser panel triggers a
directory listing refresh but provides no visual feedback that an action
occurred. The button appears static.

Solution (closing comment):
Fixed in v1.7.1.

In FileBrowser.svelte, a `refreshing` boolean state variable was added. The
refresh button now calls `handleRefresh()` instead of `onRefresh()` directly:

  async function handleRefresh() {
    if (refreshing) return;
    refreshing = true;
    await Promise.all([onRefresh(), new Promise(r => setTimeout(r, 500))]);
    refreshing = false;
  }

`Promise.all` ensures the animation always completes at least one full rotation
(500 ms minimum), even if the directory listing returns faster.

CSS `@keyframes spin-once` rotates the SVG 360° over 500 ms (linear). The
`spinning` class is added to the button while `refreshing` is true via
`class:spinning={refreshing}`. `transform-origin: center` is set on the SVG so
it rotates around its own centre.
Multiple rapid clicks are ignored while a refresh is already in progress.

---

#56
Title: [Feature] Option to disable automatic transfer queue expansion on each transfer
Labels: feature, wont-fix
Version fixed: —
Description:
A request to add an on/off toggle in Settings > Transfers that would let the
user choose whether the transfer queue panel opens automatically each time a
transfer is initiated (current behaviour) or stays hidden until opened manually.

Investigation:
The queue panel is opened via direct `queueVisible.set(true)` calls in
FileBrowser.svelte at six separate call sites — one per transfer entry point
(transferSelected, transferEntry, resolveConflict replace, advanceRename
completion, drag-and-drop drop handler, and the keyboard shortcut path). These
calls happen at the moment the transfer is queued in the frontend, before the
Go `transfer:added` event is emitted. Any toggle logic placed in the Wails event
handler is therefore never reached for the normal transfer flows.

Attempts to route all six call sites through a shared `openQueueIfEnabled()`
helper that checks a settings store value were made, but the approach was
abandoned because the store subscription and the Wails event timing made the
behaviour unreliable and the fix was more complex than the feature warranted.

Solution (closing comment):
Not implemented — marked wont-fix for now. The transfer queue always opens
automatically on each transfer. A future refactor that centralises the "queue a
transfer" action into a single function would make this toggle straightforward
to add.

---

#57
Title: [Bug] Tab key cycles through entire application while a popup is open
Labels: bug, ui, accessibility
Version fixed: v1.7.2
Description:
When a popup or overlay is open (Site Manager modal, Settings panel, password
prompt, keep-or-replace dialog, delete confirmation, conflict resolution dialog,
or disconnect-all confirmation), pressing Tab moves focus to elements behind the
popup rather than cycling within it. This makes keyboard navigation unusable
while a modal is active.

Solution (closing comment):
Fixed in v1.7.2.

A reusable Svelte action `trapFocus` was created in
`frontend/src/utils/focusTrap.js`. It listens for `keydown` on the container
node and intercepts Tab / Shift+Tab:

  - Collects all focusable elements inside the container via
    `querySelectorAll('button:not([disabled]), [href], input:not([disabled]),
    select:not([disabled]), textarea:not([disabled]),
    [tabindex]:not([tabindex="-1"])')`.
  - On Tab: if focus is on the last element (or outside the container), wraps to
    the first.
  - On Shift+Tab: if focus is on the first element (or outside the container),
    wraps to the last.
  - Cleans up via the Svelte action `destroy()` callback.

`use:trapFocus` was applied to six containers:
  - `SiteManager.svelte` — `.modal` (main site manager)
  - `SiteManager.svelte` — `.pwd-box` in the keep-or-replace overlay
  - `SiteManager.svelte` — `.pwd-box` in the password prompt overlay
  - `SettingsPanel.svelte` — `.settings-panel`
  - `App.svelte` — `.dc-box` (disconnect-all confirmation)
  - `FileBrowser.svelte` — `.confirm-box` (delete confirmation dialog)
  - `FileBrowser.svelte` — `.confirm-box` (conflict resolution dialog)

---

#58
Title: [Feature] Show transfer direction (local ↔ server) and average speed in transfer queue
Labels: feature, ui
Version fixed: v1.7.2
Description:
The transfer queue rows show the filename, size, and status, but give no
indication of which direction a file is moving (local → remote or remote →
local) nor which server is involved. The "Successful" tab also lacks a summary
of how fast the transfer completed overall.

Solution (closing comment):
Fixed in v1.7.2.

**Go side:**
- `Job` struct in `internal/transfer/queue.go` gained a new field:
  `RemoteHost string \`json:"remoteHost"\``
- `queue.Add()` signature updated to accept `remoteHost string` as a fourth
  argument.
- `manager.GetActiveHost()` added to `internal/connection/manager.go` — returns
  `cfg.Host` of the currently active connection.
- `app.QueueUpload` and `app.QueueDownload` in `app.go` now pass
  `a.connMgr.GetActiveHost()` when calling `queue.Add()`.

**Frontend — TransferQueue.svelte:**
- `dirLabel(job)` helper: returns `"local → <host> "` for uploads and
  `"<host> → local "` for downloads (trailing space separates it from the
  optional speed badge).
- `avgSpeed(job)` helper: computes average transfer speed as
  `job.size / ((finishedAt − createdAt) / 1000)` bytes/sec, then formats it
  with the existing `formatSpeed()` function. Returns `''` if data is missing
  or duration ≤ 0.
- A `.job-route` line is rendered under the filename row in all three tabs
  (pending, failed, successful) showing the direction string.
- In the "successful" tab only, if `avgSpeed(job)` is non-empty, an
  `.avg-speed` span is appended: `• <speed> <avgSuffix>` where `avgSuffix` is
  an i18n key (`"moy."` in French, `"avg."` in English).

---

## Issue #59

Title: Quick Connect button in the connection bar

Labels: enhancement

Description:
Add a button next to the Disconnect button in the connection bar so the user
can launch a new connection quickly without going through the Site Manager.

Expected behavior:
- The button shows an up arrow when connected but quick connect mode is not
  active.
- Clicking it unlocks all input fields (host, user, password, port, protocol)
  and changes the arrow to a right arrow.
- The user fills in the fields and clicks the right arrow to connect.
- Clicking outside the connection bar cancels quick connect mode and restores
  the values from the active connection.
- If one or more connections are already open, the same Keep / Replace dialog
  as in the Site Manager must appear.
- If the host field is empty when the user clicks the connect arrow, the host
  input border blinks red 3 times (CSS animation, no layout shift).

Solution (closing comment):
Fixed in v1.7.3.

**Go side:**
- New method `app.ConnectAdditional(cfg connection.Config) (connection.ConnInfo, error)`
  in `app.go` — calls `manager.ConnectNew(cfg, name)` to add a connection
  alongside existing ones, then calls `queue.SetExecutor`. Used instead of the
  existing `Connect` (which would disconnect the current connection first).
- Binding added manually to `frontend/wailsjs/go/main/App.js`.

**Frontend — connection.js:**
- New store import: `ConnectAdditional` added to wailsjs imports.
- New function `addConnectionAdHoc(cfg)`: calls `ConnectAdditional`, updates
  `connections` store + `activeConnectionId`, calls `refreshRemote('/')`. On
  error, restores `connectionStatus` to `'connected'` if other connections are
  still open (does not wipe existing tabs on failure).

**Frontend — ConnectionBar.svelte:**
- New state: `quickConnectMode` (bool), `pendingQuickCfg` (stores typed values
  across a multi-disconnect flow), `quickConnectDialog` (null or `{ cfg }`),
  `hostError` (triggers blink animation), `connBarRef` (ref to bar root),
  `hostInputRef` (ref to host input for programmatic focus).
- `enterQuickConnect()`: sets `quickConnectMode = true`, clears all fields,
  focuses the host input after a `setTimeout(..., 0)`.
- `exitQuickConnect()`: sets `quickConnectMode = false`, restores fields from
  `$activeConnectionConfig`.
- `handleQuickConnect()`: validates that host is non-empty (sets `hostError =
  true` for 1.3 s on empty); if 2+ connections open, stores `pendingQuickCfg`
  and calls `onMultiDisconnect()`; otherwise shows `quickConnectDialog = { cfg }`.
- `doQuickReplace(cfg)`: calls `addConnectionAdHoc(cfg)` (new connection
  connects first), then `closeTab(oldId)` (safe replace — old tab preserved
  until new connection succeeds).
- `doQuickOpenTab(cfg)`: calls `addConnectionAdHoc(cfg)` only (adds alongside).
- `handleDocMousedown(e)`: `document.addEventListener('mousedown', ...)` with
  `connBarRef.contains(e.target)` guard — click outside exits quick connect mode
  or dismisses the dialog.
- Reactive guard: `$: if (!isConnected && pendingQuickCfg)` restores typed
  values after a multi-disconnect-all completes.
- Reactive port guards prevent port 22 being set when `protocol === 'ftp'` in
  quick connect mode (matches existing ConnectionBar behavior).
- All inputs get `disabled={(isConnected && !quickConnectMode) || isConnecting}`
  so they remain locked when connected but unlock during quick connect.
- `use:trapFocus` applied to the quick connect dialog.

**CSS — host error blink animation:**
The `.host-error` class is added for 1.3 s then removed. The animation
intentionally also covers `.host-error:focus` so the blink appears even if
the input is focused (animation has higher cascade priority than `:focus`):

  .host-error, .host-error:focus {
    animation: host-error-blink 1.2s ease-in-out forwards;
  }
  @keyframes host-error-blink {
    0%     { border-color: var(--danger); }
    16.67% { border-color: var(--border); }
    33.33% { border-color: var(--danger); }
    50%    { border-color: var(--border); }
    66.67% { border-color: var(--danger); }
    83.33% { border-color: var(--border); }
    100%   { border-color: var(--border); }
  }

**Dialog positioning note:**
The quick connect dialog uses `position: fixed; top: 48px; right: 12px;
z-index: 300` because `.topbar-center` in App.svelte has `overflow: hidden` —
any `position: absolute` element inside it would be clipped. `position: fixed`
escapes all parent `overflow: hidden` constraints.

**i18n:** `quickConnect` key added to both `en.js` (`'Quick connect'`) and
`fr.js` (`'Connexion rapide'`).

---

#60
Title: [Feature] Duplicate site button in Site Manager
Labels: enhancement, ui
Version fixed: v1.7.3
Description:
In the Site Manager there is no way to duplicate an existing site. A "Duplicate"
button would allow the user to quickly clone a site and edit only the fields that
differ (e.g. a second server with the same credentials but a different host).
Ref: _images/issues/issue-25.png

Solution (closing comment):
Implemented in v1.7.3 in SiteManager.svelte.

A `duplicateSite()` function was added to the script:

  async function duplicateSite() {
    const copy = { ...form, name: form.name + ' (copie)' };
    await CreateSite(copy);
    await loadSites();
  }

It clones the current form fields (all values including protocol, host, port,
auth type, SSH key, remote dir, note) and creates a new site whose name is the
original name suffixed with " (copie)". The site list is reloaded immediately
so the copy appears in the left panel.

A "Dupliquer" / "Duplicate" button was added to the `.form-actions` row in
`editMode`, after the existing "Fermer" / "Close" button, pushed to the far
right with `margin-left: auto`. The button is only rendered when editing an
existing site (`{#if selectedSite}`) — it is hidden when creating a brand-new
site since there is nothing to clone.

i18n: `duplicateSite` key added to both `en.js` (`'Duplicate'`) and
`fr.js` (`'Dupliquer'`).

---

#61
Title: [Feature] Right-click context menu (cut/copy/paste) on Site Manager form inputs
Labels: enhancement, ui
Version fixed: v1.7.3
Description:
WebKit-GTK suppresses the native browser context menu inside the WebView.
In the Site Manager form, right-clicking any input field does nothing — there is
no way to cut, copy, or paste text using the mouse.

Expected behaviour:
- All text inputs (Site name, Host, Port, User, Default remote directory,
  Note, SSH key path): right-click shows Cut / Copy / Paste.
  Cut and Copy are only shown when text is selected.
- Password input: right-click shows Paste only (to avoid exposing a stored
  password via Copy or Cut).

Solution (closing comment):
Implemented in v1.7.3 in SiteManager.svelte.

A `ctxMenu` state variable replaces the per-field approach:
  `ctxMenu = { x, y, field, selStart, selEnd, value, pasteOnly }`

`handleCtxMenu(e, field, pasteOnly = false)` is called via `on:contextmenu`
on each form input. It captures the cursor/selection position from
`el.selectionStart` / `el.selectionEnd` (wrapped in try-catch for
`type="number"` inputs which throw on selection access in some engines),
then stores the element value as a string snapshot.

Three action functions:
- `ctxCopy()`: writes `value.slice(selStart, selEnd)` to the clipboard via
  `navigator.clipboard.writeText()`.
- `ctxCut()`: same write + removes the selected range from `form[field]`.
- `ctxPaste()`: reads the clipboard via `navigator.clipboard.readText()`,
  inserts the text at the cursor position (replacing any selection).
  For `field === 'port'`, the result is parsed with `parseInt()` to keep
  `form.port` as a number.

The menu HTML reuses the existing `.paste-ctx-menu` CSS class. Cut and Copy
buttons are rendered only when `ctxMenu.selStart !== ctxMenu.selEnd`. The
Paste button is always shown.

`on:contextmenu` handlers added to:
- `form.name` input → `handleCtxMenu(e, 'name')`
- `form.host` input → `handleCtxMenu(e, 'host')`
- `form.port` input → `handleCtxMenu(e, 'port')`
- `form.user` input → `handleCtxMenu(e, 'user')`
- `form.password` input → `handleCtxMenu(e, 'password', true)` (paste only)
- `form.sshKeyPath` input → `handleCtxMenu(e, 'sshKeyPath')`
- `form.remoteDir` input → `handleCtxMenu(e, 'remoteDir')`
- `form.note` textarea → `handleCtxMenu(e, 'note')`

`<svelte:window on:click>` updated to reset both `pasteMenu` and `ctxMenu`.

i18n: `cut` and `copy` keys added to both `en.js` and `fr.js`.
The existing `paste` key is reused.

---

#62
Title: [Feature] Password visibility toggle (eye icon) on the Site Manager form password field
Labels: enhancement, ui
Version fixed: v1.7.3
Description:
In the Site Manager form, when editing a saved site, the "Password" input has no
eye icon to reveal/hide the typed password. Users cannot verify what they have
typed before saving.
(The ask-password prompt overlay already has this toggle — issue #29.)

Solution (closing comment):
Implemented in v1.7.3 in SiteManager.svelte.

A `showFormPwd` boolean state variable was added (separate from `showPwd` which
controls the ask-password prompt overlay).

The single `<input type="password">` was replaced with a `.pwd-input-wrap` div
containing two inputs in an `{#if}/{:else}` block — one `type="text"` and one
`type="password"`, both bound to `form.password` — and an `.eye-btn` that
toggles `showFormPwd`. The `{#if}/{:else}` pattern is required because Svelte 3
does not allow a dynamic `type` attribute on a two-way-bound input (WebKit-GTK
pattern, see CLAUDE.md).

The existing `.pwd-input-wrap`, `.pwd-input`, and `.eye-btn` CSS classes are
reused directly — no new styles needed. The `on:contextmenu` paste-only handler
from issue #61 is preserved on both input variants.

---

#63
Title: [UI] Site Manager action buttons (Connect, Edit, Delete) left-aligned instead of centered
Labels: bug, ui
Version fixed: v1.7.3
Description:
In the Site Manager detail panel, the three action buttons — "Connecter",
"Modifier le site", and "Supprimer le site" — are left-aligned. They should be
centered horizontally in the detail area.
Ref: _images/issues/v1.7.3/issue-26.png

Solution (closing comment):
Fixed in v1.7.3 in SiteManager.svelte.

Added `justify-content: center` to the `.view-actions` CSS rule:

  .view-actions { display: flex; flex-wrap: wrap; gap: 8px; align-items: center; justify-content: center; }

---

#64
Title: [Security] Passwords stored in plain text in sites.json
Labels: enhancement, security
Version fixed: v1.7.3
Description:
Saved site passwords are written in plain text in the config file
~/.config/GlideFTP/sites.json. Anyone with access to the file (backup, sync,
export) can read all credentials without any protection.

Solution (closing comment):
Implemented in v1.7.3. Passwords are now stored in the OS system keyring and
never written to disk.

Backend - new packages:
- `internal/keyring/keyring.go` - wraps github.com/zalando/go-keyring.
  Uses the D-Bus Secret Service API (gnome-keyring / kwallet) on Linux and
  the Windows Credential Manager (DPAPI) on Windows. No TPM required on
  either platform. `IsAvailable()` probes the daemon at startup.
- `internal/crypto/crypto.go` - .gfe encrypted export format.
  Key derivation: Argon2id (memory=64 MB, time=3, threads=4) -> 32-byte key.
  Encryption: AES-256-GCM. File layout: [4B magic "GFEX"][1B version]
  [32B random salt][12B random nonce][ciphertext + GCM tag].

Backend - app.go changes:
- `sites.go save()` always strips passwords before writing to disk.
- `migratePasswords()` runs at startup: any plaintext password still present in
  an old sites.json is migrated to the keyring, then the file is re-saved stripped.
- `GetSites()` enriches each site with its password from the keyring before
  returning to the frontend (so the form still shows the saved password).
- `CreateSite` / `UpdateSite`: password is extracted, cleared from the struct,
  then stored in the keyring. Auth types anonymous / ask_password / interactive
  never store a password (handled by `needsKeyring()`).
- `DeleteSite`: also calls `keyringMgr.Delete()`.
- `buildSiteConfig`: fetches the password from the keyring for each connection.
- New bindings: `GetKeyringStatus()`, `ExportSitesPlain()`,
  `ExportSitesEncrypted(passphrase)`, `OpenImportDialog()`, `DoImportSites(path, passphrase)`.

Frontend - SiteManager.svelte:
- Yellow warning banner shown when `GetKeyringStatus()` returns
  `"keyring_unavailable"`, with a message asking the user to install
  gnome-keyring or use "Ask password" auth.
- Export button now opens a two-step dialog:
  - Step 1: choice between "Without passwords" (plain .json) and
    "With passwords (encrypted)" (.gfe).
  - Step 2 (encrypted only): passphrase input + confirmation field, both
    with eye-toggle. Mismatched or empty passphrase is blocked client-side.
- Import: `OpenImportDialog()` opens the file picker and returns
  `{ path, needsPassphrase }`. If the file is a .gfe, a passphrase prompt
  is shown before calling `DoImportSites(path, passphrase)`. Plain JSON
  imports (including old-format files with plaintext passwords) are handled
  transparently - passwords found in the JSON are stored in the keyring on
  import.

Cross-platform export/import:
The .gfe format uses only Go stdlib crypto primitives - it is fully portable
between Linux and Windows. A file exported on Linux can be imported on Windows
and vice versa.

Linux - keyring availability (Arch):
Arch users on minimal setups may not have gnome-keyring or kwallet installed.
A README.md is included in the Arch AppImage archive with check and install
commands. If no keyring daemon is running, the banner guides the user to use
"Ask password" auth instead.

---

#65
Title: [UX] No visual feedback when clicking Connect in the Site Manager
Labels: enhancement, ui
Version fixed: v1.7.3
Description:
When the user clicks "Connect" in the Site Manager on a saved site, the background
grays out slightly but the modal itself does not change. On a slow connection this
leaves no visible indication that the click was registered, causing the user to
wonder whether the action started or the application froze.
User feedback: re-clicking triggered an "already connected" error.

Solution (closing comment):
Implemented in v1.7.3 in SiteManager.svelte.

A `connecting` boolean state variable was added. All four connection paths set it
to `true` before the async call and reset it in a `finally` block:
- `connectToSite()` - direct connect (normal auth)
- `doKeepAndAdd()` - keep existing + open new tab (duplicate and non-duplicate paths)
- `doReplace()` - replace current connection
- `confirmPasswordConnect()` - ask_password prompt confirm button

While `connecting` is true:
- The Connect button (site detail panel) is disabled and shows a spinning arc SVG
  + the existing `connecting` i18n key ("Connexion..." / "Connecting...").
- The Keep / Replace buttons in the keep-or-replace overlay are both disabled and
  show the same spinner + text.
- The Connect button in the password prompt overlay is disabled and shows the
  spinner; the Cancel button is also disabled to prevent mid-connection dismissal.

CSS: `@keyframes btn-spin` (360deg, 0.75s linear) applied via `.btn-spinner` class
on a 14x14 SVG arc icon. No layout shift - the spinner replaces the arrow icon at
the same size.

---

1.7.4

#66
Title: [Feature] Accent shadow box on the connection card (main screen)
Labels: enhancement, ui
Version fixed: v1.7.4
Description:
On the main (disconnected) screen, the centered connection card has no decorative
shadow. A request to add an outer shadow on the bottom-right side using the active
accent color, with a toggle to enable or disable it in Settings > Interface
(default: off).

Solution (closing comment):
Implemented in v1.7.4.

**internal/settings/settings.go:**
- New field `ConnectCardShadow bool \`json:"connectCardShadow"\`` added to the
  Settings struct. Default value: `false` (off).

**frontend/src/stores/settings.js — applyAccentColor():**
- New CSS custom property `--accent-glow` added alongside `--accent-subtle`:
  `rgba(r, g, b, 0.45)`. This gives a semi-transparent variant of the accent
  color usable for glow effects without hardcoding opacity in component CSS.

**frontend/src/App.svelte:**
- `settings` store imported from `stores/settings.js`.
- `.connect-card` receives `class:shadow-accent={$settings?.connectCardShadow}`.
- CSS `.connect-card.shadow-accent`:
    box-shadow:
      0 8px 32px rgba(0,0,0,0.3),      /* existing dark shadow preserved */
      4px 4px 8px var(--accent-glow),   /* close neon halo */
      8px 8px 20px var(--accent-glow),  /* medium neon halo */
      14px 14px 45px var(--accent-subtle); /* soft far diffusion */
  Three layered shadows offset to the bottom-right create a neon/glow look
  rather than a hard opaque shadow. `transition: box-shadow 0.25s ease` animates
  the toggle.

**frontend/src/components/SettingsPanel.svelte:**
- New toggle switch row added as the first item in the Interface section:
  label `$t('connectCardShadow')`, bound to `form.connectCardShadow` via the
  existing `toggle()` helper.

**i18n:**
- `connectCardShadow`: `'Connection card accent shadow'` (EN) /
  `'Ombre d\'accentuation du panneau de connexion'` (FR).

---

#67
Title: [Feature] Tree view for local and remote file panels
Labels: enhancement, ui
Version fixed: v1.7.4
Description:
The file browser panels only show a flat directory listing. A request to add a
hierarchical tree view that shows the full folder structure, with files visible
and transferable, switchable per panel via a toggle button in the panel toolbar.

Expected behaviour:
- A toggle button to the left of the Refresh button switches the panel between
  list view (current) and tree view.
- The default view on each connection is the list view.
- Tree view starts from the filesystem root (/), lazily loading children when a
  folder is expanded.
- Folders and files are both visible in the tree (folders first, then files,
  alphabetically).
- Clicking a folder navigates to it (updates the breadcrumb and the panel path).
- Double-clicking a file (or clicking the arrow button on hover) transfers it to
  the opposite panel's current path.
- Single-clicking a file selects it with an accent highlight.
- The node matching the current path is highlighted in accent color.
- Ancestors of the current path are auto-expanded when entering tree mode.
Ref: _images/issues/v1.7.4/issue-27.png (button placement),
     _images/issues/v1.7.4/issue-28.png (tree style reference)

Solution (closing comment):
Implemented in v1.7.4 in FileBrowser.svelte.

**State:**
- `treeMode` boolean (default false, resets on component destroy / reconnect).
- `treeNodes` array: flat display list of `{path, name, depth, isDir, size,
  modTime, expanded, loading, leaf}`. Files have `leaf: true`; folder nodes
  start as non-leaf until expanded.
- `treeLoaded` Set and `treeChildrenMap` object: lazy-load cache — each dir path
  is fetched at most once per tree session.
- `treeSelected` string: path of the currently single-clicked file (for highlight).

**Functions:**
- `fetchDirChildren(dirPath)`: calls `LocalListDir` or `RemoteListDir`, returns
  sorted dirs-first + files. Result is cached in `treeChildrenMap`.
- `enterTreeMode()`: resets all tree state, loads `/` children, calls
  `autoExpandToPath(path)` to pre-expand ancestors of the current path.
- `autoExpandToPath(targetPath)`: iterates ancestor segments and calls
  `expandTreeNode(idx)` for each one that is not yet expanded.
- `expandTreeNode(idx)`: marks node as loading, fetches children, inserts child
  nodes immediately after the parent in `treeNodes`, marks parent as
  `expanded: true` (or `leaf: true` if no children).
- `collapseTreeNode(idx)`: removes all descendant nodes (depth > parent depth)
  from `treeNodes` and marks parent as `expanded: false`.
- `toggleTreeNode(idx)`: expand or collapse depending on current state.
- `transferTreeFile(node)`: queues upload or download to `otherPath` via the
  existing `QueueUpload` / `QueueDownload` bindings.

**Toggle button:**
Placed to the left of the Refresh button in `.browser-actions`. Shows a tree
icon (inactive) or list icon (active); `class:active={treeMode}` applies accent
color + subtle background when in tree mode.

**Tree row rendering (`{#each treeNodes}`):**
- Indentation: `padding-left: {8 + depth * 16}px`.
- Dir rows: expand/collapse button (▶ collapsed / ▼ expanded / spinner loading /
  `·` leaf), 📁 icon, folder name. Click → `onNavigate(node.path)`.
- File rows: spacer (no toggle button), 📄 icon, file name, file size (right-
  aligned), transfer arrow button (visible on hover). Click → select
  (`treeSelected = node.path`). Double-click → `transferTreeFile(node)`.
- `class:tree-active` on the dir matching `path` (accent highlight).
- `class:tree-selected` on the selected file (accent highlight + transfer button
  always visible).

**i18n:**
- `treeView`: `'Tree view'` (EN) / `'Vue arborescente'` (FR).
- `listView`: `'List view'` (EN) / `'Vue liste'` (FR).

---

#68
Title: [Bug] "Clear" button in the Failed tab of the transfer queue has no effect
Labels: bug
Version fixed: v1.7.4
Description:
Clicking the "Clear" (Vider) button in the "Failed" tab of the transfer queue
does not remove the entries from the list. The button appears to do nothing.
The same button works correctly in the "Successful" tab.

Root cause:
The Failed tab displays jobs with status `failed` OR `cancelled` (cancelled
transfers are grouped with failed ones in the UI):

  $: failed = $transfers.filter(j => j.status === 'failed' || j.status === 'cancelled');

However the "Clear" button only called `clearTransfers('failed')`, which maps
to `queue.Clear(StatusFailed)` on the Go side. This removes only jobs whose
status is exactly `"failed"`. Jobs with status `"cancelled"` are left untouched.

When a user cancels a transfer (via the Cancel button), its status becomes
`"cancelled"`. If the Failed tab contains only cancelled jobs (or a mix where
the failed ones are cleared but cancelled ones remain), the list appears
unchanged after clicking Clear.

Solution (closing comment):
Fixed in v1.7.4 in TransferQueue.svelte.

The "Clear" button handler for the Failed tab now calls `clearTransfers` twice,
once per status:

  if (activeTab === 'failed') {
    clearTransfers('failed');
    clearTransfers('cancelled');
  } else {
    clearTransfers('done');
  }

Each call triggers a separate `ClearTransfers` binding → `queue.Clear()` on the
Go side → emits `transfer:cleared` with the corresponding status → the frontend
event handler filters out all matching jobs from the store.
Both calls are fast in-memory operations with no network involvement.

---

#69
Title: [Bug] Concurrent transfer option has no effect — transfers happen one by one
Labels: bug
Version fixed: v1.7.4
Description:
The "Max simultaneous transfers" setting in Settings > Transfers has no visible
effect. Even when set to 3 (or more), files are transferred one at a time
regardless of the value chosen.

Two independent root causes:

1. SaveSettings() did not update the queue's semaphore.
   The transfer queue (Queue) is created at startup with a buffered semaphore
   channel sized to MaxConcurrentTransfers. When the user saves new settings,
   SaveSettings() in app.go updated the speed limit via queue.SetSpeedLimit() but
   never updated the semaphore capacity. Any change made to MaxConcurrentTransfers
   after startup was therefore silently ignored — the queue kept using the
   semaphore from startup for the lifetime of the app.

2. FTP serializes all transfers through a single global mutex.
   FTPClient.Upload() and FTPClient.Download() both acquired c.mu (a sync.Mutex)
   at the very start of the function and held it until the transfer completed.
   Because jlaffaye/ftp is not thread-safe on a single connection, the mutex was
   there to prevent data corruption — but holding it for the full duration of
   every transfer meant only one goroutine could transfer at a time, making the
   semaphore in the queue irrelevant for FTP sessions. SFTP was not affected
   (github.com/pkg/sftp natively supports concurrent requests over a single SSH
   connection via packet IDs).

Solution (closing comment):
Fixed in v1.7.4 — two separate fixes.

**Fix 1 — queue.go: SetWorkers() + run() semaphore snapshot**

A new method SetWorkers(n int) was added to Queue:

  func (q *Queue) SetWorkers(n int) {
      q.mu.Lock()
      q.workers = n
      q.sem = make(chan struct{}, n)
      q.mu.Unlock()
  }

run() was updated to snapshot q.sem into a local variable before the select,
so that both the acquire and the deferred release always use the same channel
even if SetWorkers() replaces q.sem mid-flight:

  q.mu.Lock()
  sem := q.sem
  q.mu.Unlock()
  select {
  case sem <- struct{}{}:
  ...
  }
  defer func() { <-sem }()

Without this snapshot a goroutine that acquired from the old semaphore would
have released from the new one (pointing at an empty channel) → deadlock.

**Fix 2 — app.go: propagate the setting change to the queue**

SaveSettings() now calls SetWorkers in addition to SetSpeedLimit:

  func (a *App) SaveSettings(s settings.Settings) error {
      a.appSettings = &s
      a.queue.SetWorkers(s.MaxConcurrentTransfers)
      a.queue.SetSpeedLimit(s.MaxTransferSpeedKBps)
      return s.Save()
  }

**Fix 3 — ftp.go: dedicated connection per FTP transfer**

The global mutex on Upload() and Download() was removed. Each call to Upload()
or Download() now opens its own dedicated FTP connection via a new internal
dial() helper (same credentials as the primary connection):

  func (c *FTPClient) Upload(ctx context.Context, ...) error {
      conn, err := c.dial()
      if err != nil { return err }
      defer conn.Quit()
      // ... transfer using conn, no shared lock
  }

This is the standard approach used by all major FTP clients (FileZilla, etc.):
since FTP only allows one active data transfer per control connection, N
concurrent transfers require N separate control connections. The primary
connection (c.conn) is preserved exclusively for control operations (ListDir,
MkDir, Delete, Rename, CurrentDir) which continue to be serialized by c.mu as
before.

---

#70
Title: [Bug] Transferring a folder fails with "550 Not a regular file"
Labels: bug
Version fixed: v1.7.4
Description:
Attempting to transfer a folder (either upload or download) results in the
transfer immediately failing with the error:

  550 /path/to/Folder: Not a regular file

The error appears on every retry attempt, keeping the entry permanently in the
"Failed" tab.

Root cause:
QueueUpload() and QueueDownload() passed the path directly to executor.Upload()
/ executor.Download(), which call conn.Stor() / conn.Retr() (FTP) or
client.Create() / client.Open() (SFTP) on the given path. Both FTP and SFTP
protocols only allow RETR/STOR on regular files; issuing them on a directory
path causes the server to return 550. There was no check for whether the
queued path was a file or a directory — folders were silently treated as files
and the transfer always failed.

Solution (closing comment):
Fixed in v1.7.4.

Two new Go methods were added to app.go for recursive directory transfers:

**QueueUploadDir(localPath, remotePath string)**
Runs asynchronously (goroutine). Calls enqueueUploadDir() which:
1. Creates the remote directory via connMgr.MkDir(remotePath).
2. Lists the local directory with os.ReadDir(localPath).
3. For each file entry: queue.Add(Upload, src, dst, host).
4. For each subdirectory: recurse into enqueueUploadDir(src, dst).

**QueueDownloadDir(remotePath, localPath string)**
Runs asynchronously (goroutine). Calls enqueueDownloadDir() which:
1. Creates the local directory with os.MkdirAll(localPath, 0755).
2. Lists the remote directory via connMgr.ListDir(remotePath).
3. For each file entry: queue.Add(Download, dst, e.Path, host).
4. For each subdirectory: recurse into enqueueDownloadDir(e.Path, dst).

The result is one transfer queue job per file in the folder tree, each
independently visible, cancellable, and retryable in the queue panel.

Both methods were exported to the frontend via new entries in
frontend/wailsjs/go/main/App.js.

**FileBrowser.svelte — doQueueTransfer():**
The function now checks entry.isDir before deciding which binding to call:

  if (entry.isDir) {
    if (side === 'local') QueueUploadDir(entry.path, dest);
    else QueueDownloadDir(entry.path, dest);
  } else {
    if (side === 'local') QueueUpload(entry.path, dest);
    else QueueDownload(entry.path, dest);
  }

**FileBrowser.svelte — drag-and-drop:**
handleDragStart() now includes isDir in the serialized drag payload:
  entries: dragEntries.map(en => ({ path, name, isDir: en.isDir }))
handleDrop() reads isDir from each entry and routes to QueueUploadDir /
QueueDownloadDir / QueueUpload / QueueDownload accordingly.

---

#71
Title: [Bug] Connection drops silently after a few minutes; timeout setting has no effect on idle
Labels: bug
Version fixed: v1.7.4
Description:
Several related problems reported together:

1. The connection drops silently after 5-10 minutes of inactivity regardless
   of the timeout value set in Settings. When it happens the UI gives no
   feedback — files and folders can no longer be clicked or transferred, but
   no error message appears and no automatic disconnect occurs.

2. Setting 50 minutes (3000 seconds) in the timeout field does not help — the
   connection still drops after only 5-10 minutes.

3. The user asked whether moving the mouse resets the idle countdown.

Root cause:
The "Connection timeout (seconds)" setting controls the TCP dial timeout and
the ListDir operation timeout — it is NOT an idle inactivity timer. It has no
effect on how long an idle connection stays open.

The actual cause of the drops is the FTP/SFTP server's own idle timeout
(typically 5-15 minutes). The server closes the TCP socket after a period of
inactivity on the control connection. GlideFTP was sending nothing to keep the
connection alive, so the server would silently drop it.

Moving the mouse does NOT reset the server's idle timer — only network activity
on the control connection does. Without keepalives, the connection will always
drop regardless of what the user does on the client side.

The default connection timeout was also set too low (30 seconds) for an
operation timeout, making slow servers appear to time out during normal use.

Solution (closing comment):
Fixed in v1.7.4 — three separate changes.

**1. Default timeout raised to 15 minutes (internal/settings/settings.go)**
ConnectionTimeoutSec default changed from 30 to 900. This applies to:
- The TCP dial timeout when establishing a new connection.
- The ListDir operation watchdog (the guard that disconnects if a directory
  listing hangs for longer than the configured value).

**2. Keepalive goroutine — prevents server-side idle drops (internal/connection/)**

A Keepalive() method was added to the Client interface and implemented by both
clients:

FTPClient.Keepalive() (ftp.go):
  Sends a NOOP command on the primary control connection.
  Uses sync.Mutex.TryLock() so it never blocks if another control operation
  (ListDir, MkDir, etc.) already holds the lock — in that case the server is
  clearly active and the keepalive tick is safely skipped.

SFTPClient.Keepalive() (sftp.go):
  Sends an SSH global request "keepalive@openssh.com" on the underlying SSH
  connection. OpenSSH responds with a failure reply (not an error) if it does
  not recognise the request type; what matters is that the TCP roundtrip
  succeeds. If the TCP socket is dead the call returns an error immediately.

Manager.startKeepalive() (manager.go):
  Called automatically after every successful connection (doConnect()).
  Runs a goroutine with a 60-second ticker:
  - Each tick checks whether the connection ID is still present in the
    manager's active list; if not (normal user disconnect), the goroutine
    exits cleanly without calling onLost.
  - If the ID is still present, calls client.Keepalive().
  - On keepalive error: removes the entry from the manager, updates activeID
    if needed, and calls onLost(id, host) only if the entry was actually
    found and removed (guards against a race with a concurrent normal
    disconnect).

**3. User notification on unexpected disconnect (app.go + App.svelte)**

app.go startup():
  Registers a SetOnConnectionLost callback on the manager that emits a Wails
  event "connection:lost" with { id, host } when a keepalive detects a dead
  connection.

App.svelte onMount():
  Subscribes to "connection:lost" via EventsOn:
  - Calls closeTab(id) to remove the dead tab and update all connection
    stores (identical to a normal user-initiated tab close).
  - Sets lostNotif = { host }, showing a dismissible red banner:
    "⚠ Connection lost — The connection to <host> was dropped by the server.
    Please reconnect."
  - The banner auto-dismisses after 10 seconds; the ✕ button dismisses it
    immediately.

i18n: connectionLost / connectionLostDetail keys added to en.js and fr.js.

---

#72
Title: [Feature] Intra-panel drag-and-drop, copy and cut/paste for local and remote panels
Labels: enhancement
Version fixed: v1.7.4
Description:
No way to copy, move, or reorganise files within the same panel. Requested
behaviours:

- Drag a file or folder onto a subfolder or the ".." row within the same panel
  to move it there.
- Ctrl+C / Ctrl+X / Ctrl+V (and right-click Copy / Cut / Paste) to copy or
  move files and folders within the local panel or within the remote panel.
- Works in both list view and tree view.
- Local → Local: real recursive copy (no network involved).
- Remote → Remote: transparent copy via temp-file download + re-upload (no
  native FTP/SFTP server-side copy command exists).
- Cross-panel operations are transfers, not copies — not in scope here.
- Auto-refresh while the operation is running (first at +1 s, then every 4 s),
  plus a final refresh when done.
- Green confirmation banner on success, orange on failure, auto-dismissed
  after 4 seconds.

Solution (closing comment):
Implemented in v1.7.4.

**Go backend:**

internal/fs/local.go — Copy(src, dst string) error:
  Recursive local copy. Detects file vs directory via os.Stat. For files:
  os.Open → os.Create → io.Copy. For directories: os.MkdirAll + recurse into
  each entry. Used by the new app.LocalCopy binding.

internal/connection/manager.go — CopyRemote / CopyRemoteDir:
  CopyRemote(srcPath, destPath): downloads the remote file to a OS temp file
  (os.CreateTemp), re-uploads it to the destination, then deletes the temp
  file. Works for both FTP and SFTP without requiring any server-side COPY
  command. Runs synchronously so the JS caller can await completion.
  CopyRemoteDir(srcPath, destPath): synchronous recursive variant — MkDir at
  destination, ListDir at source, recurse into subdirectories, CopyRemote each
  file. Synchronous so that onRefresh() in the frontend fires only after the
  full tree is copied.

app.go: LocalCopy, RemoteCopy, RemoteCopyDir — thin wrappers over the above.

frontend/wailsjs/go/main/App.js: LocalCopy, RemoteCopy, RemoteCopyDir
  bindings added.

**Frontend — clipboard store (connection.js):**
  export const clipboard = writable(null);
  Shape: { entries: [{path, name, isDir}], operation: 'copy'|'cut',
           side: 'local'|'remote' }
  Shared between both FileBrowser instances so a clipboard set in one panel is
  visible to the other (though paste is rejected when side !== currentSide).
  Cleared automatically after a successful cut+paste.

  New helpers: localCopy(src, dest), remoteCopy(src, dest),
  remoteCopyDir(src, dest).

**Frontend — FileBrowser.svelte:**

Keyboard (handlePanelKeydown):
  Ctrl+C → copyEntries(selected) — sets clipboard with operation 'copy'.
  Ctrl+X → cutEntries(selected) — sets clipboard with operation 'cut'.
  Ctrl+V → pasteToFolder(path) if clipboard.side === side.

Right-click context menu:
  Cut and Copy buttons always visible on file/folder items.
  Paste button visible only when $clipboard.side === side.
  Paste routes to pasteToFolder(contextEntry.isDir ? contextEntry.path : path)
  so right-clicking a subfolder pastes directly into it.

pasteToFolder(destFolder):
  Iterates clipboard.entries. For each entry:
    cut  → onRename(src, dest)       (local or remote move via rename)
    copy + local → localCopy(src, dest)
    copy + remote file → remoteCopy(src, dest)
    copy + remote dir  → await remoteCopyDir(src, dest)
  Progressive refresh: setTimeout 1 s → onRefresh(), then setInterval 4 s →
  onRefresh() while the operation is running. Both timers cancelled (stopProgress)
  before the final onRefresh() + success/failure banner.
  Success banner (green): "Copie(s) terminée(s)" / "Collage(s) terminé(s)".
  Error banner (orange): error message from Go.
  Both auto-dismiss after 4 s.

Intra-panel drag-and-drop (list view + tree view):
  rowDragOverPath state drives a .drag-target CSS class (accent outline) on
  the hovered folder row or ".." row.
  handleRowDragOver(e, folderPath): e.preventDefault() + e.stopPropagation()
  + set rowDragOverPath.
  handleDropOnFolder(e, destFolder): reads drag data; if fromSide === side →
  calls onRename for each entry (move); if fromSide !== side → routes to
  QueueUpload/QueueDownload/QueueUploadDir/QueueDownloadDir into that specific
  subfolder (bonus: cross-panel drops can now target a specific subfolder, not
  just the current directory).
  ".." row gets drag handlers with parentPath as destination.
  Tree dir nodes get draggable + dragstart (packs node into drag payload) +
  dragover/dragleave/drop handlers.

i18n: pasteHere, copyDone, cutDone added to en.js and fr.js.

---

#73
Title: [Feature] Deleted local files go to system trash / recycle bin instead of being permanently deleted
Labels: enhancement
Version fixed: v1.7.4
Description:
When deleting a file or folder from the local panel (Delete key, right-click
→ Delete, or multi-selection delete), the item disappears permanently. It does
not appear in the OS trash / recycle bin, making recovery impossible without
a third-party tool.

Expected behaviour: deleting from the local panel should move the item to the
system trash (Linux) or Recycle Bin (Windows), just like deleting from a
standard file manager.

Note: this feature only applies to the local panel. Remote files (FTP/SFTP)
are deleted on the server and there is no server-side trash mechanism in
either protocol.

Solution (closing comment):
Fixed in v1.7.4.

Three platform-specific files were added to internal/fs/ using Go build tags.
app.go LocalDelete() now calls localfs.Trash() instead of localfs.Delete().

**Linux — internal/fs/trash_linux.go (//go:build linux)**

Implements the FreeDesktop.org XDG Trash specification:

1. Determines the trash directory:
   $XDG_DATA_HOME/Trash, falling back to ~/.local/share/Trash.
2. Creates files/ and info/ subdirectories with mode 0700 if absent.
3. Resolves a unique name for the trashed item (collision avoidance:
   appends .1, .2, ... before the extension until a free slot is found).
4. Moves the item via os.Rename (fast, atomic, no data copy).
   Cross-device case (EXDEV): falls back to localfs.Copy() + os.RemoveAll()
   so files on separate filesystems (e.g. a second partition) are still
   handled correctly.
5. Writes the .trashinfo sidecar file:
   [Trash Info]
   Path=/absolute/original/path
   DeletionDate=2024-06-01T14:30:00

The trashinfo format is compatible with Nautilus, Thunar, Dolphin, Nemo,
and any other FreeDesktop-compliant file manager.

**Windows — internal/fs/trash_windows.go (//go:build windows)**

Calls SHFileOperationW from shell32.dll via syscall.NewLazyDLL (no CGo
required, compatible with mingw-w64 cross-compilation from Linux).

The SHFILEOPSTRUCTW struct is reproduced in Go with explicit padding fields
to match MSVC's 64-bit layout (56 bytes total):
  hwnd, wFunc, _pad1, pFrom, pTo, fFlags, _pad2, fAborted, hMapping, title.

Flags used:
  FOF_ALLOWUNDO      (0x0040) — sends to Recycle Bin, recoverable
  FOF_NOCONFIRMATION (0x0010) — no Windows dialog
  FOF_SILENT         (0x0004) — no progress window
  FOF_NOERRORUI     (0x0400) — suppress error dialogs (errors returned as int)

The pFrom path is encoded as a double-null-terminated UTF-16 multi-string
(required by SHFileOperationW): unicode/utf16.Encode([]rune(path + "\x00\x00")).

**Fallback — internal/fs/trash_other.go (//go:build !linux && !windows)**

Falls back to permanent deletion (os.RemoveAll) on unsupported platforms so
the package still compiles everywhere.

---

1.7.5

#74
Title: [Bug] Right-click "Transfer" in context menu ignores multi-selection — only transfers the right-clicked file
Labels: bug
Version fixed: v1.7.5
Description:
When multiple files are selected and the user right-clicks one of them and
chooses "Transfer", only the right-clicked file is transferred. The rest of
the selection is ignored.
The toolbar Transfer button (↗) correctly transfers all selected files, so
the issue is specific to the context menu path.

Solution (closing comment):
Fixed in v1.7.5 in FileBrowser.svelte.

Root cause: the context menu "Transfer" button called `transferEntry(contextEntry)`
directly, which only handles a single entry, ignoring the selection entirely.
The Cut, Copy, and Delete entries in the same menu already used the correct
pattern:
  `selected.some(s => s.path === contextEntry?.path) ? selected : [contextEntry]`
but Transfer did not.

Fix: a shared `transferEntries(entries)` function was factored out (holds the
conflict-check + queue logic). `transferSelected()` and `transferEntry()` both
delegate to it. The context menu button was changed to:
  const ents = selected.some(s => s.path === contextEntry?.path)
               ? selected : [contextEntry];
  closeContext(); transferEntries(ents);
This makes Transfer consistent with Cut / Copy / Delete.

---

#75
Title: [Bug] Refresh button does nothing in tree view
Labels: bug
Version fixed: v1.7.5
Description:
In tree view mode, clicking the Refresh button (↻) has no visible effect.
The tree stays stale even after creating, deleting, or renaming files on the
server from another client.
In list view mode the button works correctly.

Solution (closing comment):
Fixed in v1.7.5 in FileBrowser.svelte.

Root cause: `handleRefresh()` called `onRefresh()`, which refreshes only the
flat `entries` list used by the list view. The tree data (`treeNodes`,
`treeLoaded`, `treeChildrenMap`) is independent and was never touched.

Fix: `handleRefresh()` now detects `treeMode` and branches:

  async function handleRefresh() {
    if (refreshing) return;
    refreshing = true;
    if (treeMode) {
      await Promise.all([enterTreeMode(), new Promise(r => setTimeout(r, 500))]);
    } else {
      await Promise.all([onRefresh(), new Promise(r => setTimeout(r, 500))]);
    }
    refreshing = false;
  }

`enterTreeMode()` clears `treeLoaded`, `treeChildrenMap`, and `treeNodes`, then
re-fetches from `/` and re-expands the current path — a full in-place rebuild
that preserves the spinner animation timing.

---

#76
Title: [UI] Site Manager detail panel content not centered when many sites are listed
Labels: bug, ui
Version fixed: v1.7.5
Description:
In the Site Manager, when many sites fill the left panel, the right-side
detail area (site name, protocol/host, note, and action buttons) is
left-aligned instead of being horizontally and vertically centered.
The "no selection" state (placeholder icon) is correctly centered, but the
selected-site view is not.
Ref: _images/issues/v1.7.5/issue-30.jpg

Solution (closing comment):
Fixed in v1.7.5 in SiteManager.svelte.

Three CSS changes:
- `.site-detail`: added `display: flex; flex-direction: column;
  align-items: center; justify-content: center;` so the content is centered
  in both axes within the right panel.
- `.site-view`: added `align-items: center; width: 100%;` so each child
  element centers horizontally while the wrapper keeps its full width.
- `.site-view-header`: added `align-items: center; text-align: center;`
  so the site name and subtitle text are centered.
Modal width increased from 700 px to 780 px to give the three action buttons
comfortable horizontal room when rendered side by side.

---

#77
Title: [Feature] Configurable window size and maximized-on-startup option in Settings
Labels: enhancement
Version fixed: v1.7.5
Description:
The GlideFTP window always opens at a fixed size (1400×900). Users who prefer
a smaller window or a maximized start have no way to configure this without
editing internal files.
Requested: a width stepper, a height stepper, and an "Open maximized" toggle
in Settings > Interface. When "Open maximized" is enabled, the width and height
controls should be disabled (grayed out).

Solution (closing comment):
Implemented in v1.7.5.

**internal/settings/settings.go:**
Three new fields added to the Settings struct (with defaults matching the
previously hardcoded values):
  WindowWidth  int  \`json:"windowWidth"\`   // default 1400
  WindowHeight int  \`json:"windowHeight"\`  // default 900
  StartMaximized bool \`json:"startMaximized"\` // default false

**main.go:**
Reads WindowWidth / WindowHeight from settings before calling wails.Run() so
the window opens at the right size from the first frame:
  s, _ := settings.Load()
  w, h := s.WindowWidth, s.WindowHeight
  if w <= 0 { w = 1400 }
  if h <= 0 { h = 900 }
  wails.Run(&options.App{ Width: w, Height: h, ... })

**app.go — startup():**
After other initialization, applies the stored window configuration:
  if s.StartMaximized {
    runtime.WindowMaximise(ctx)
  } else if s.WindowWidth > 0 && s.WindowHeight > 0 {
    runtime.WindowSetSize(ctx, s.WindowWidth, s.WindowHeight)
  }

**SettingsPanel.svelte — Interface section:**
Three new rows added after "Confirm before deleting":
- "Open maximized" toggle switch (`startMaximized`).
- "Window width (px)" stepper (range 800–3840, step 50),
  disabled and grayed out (`class:row-disabled`) when `startMaximized` is true.
- "Window height (px)" stepper (range 600–2160, step 50),
  same disabled behavior.
New CSS: `.row-disabled { opacity: 0.4; pointer-events: none; }`.

**i18n:** `windowWidth`, `windowHeight`, `startMaximized` keys added to
en.js and fr.js.

---

#78
Title: [Feature] "Reset to default" button for each setting in the Settings panel
Labels: enhancement, ui
Version fixed: v1.7.5
Description:
Once a setting has been changed there is no quick way to revert it to its
factory default without knowing what the default value is.
Requested: a small button next to each setting label that resets that
individual setting to its default value.
Excluded by the user: the accent color picker and the default local directory
field (those already have their own reset mechanism or are path-specific).

Solution (closing comment):
Implemented in v1.7.5 in SettingsPanel.svelte.

A `DEFAULTS` constant in the script holds the canonical default value for
every resettable key (matching `settings.Default()` in Go):
  theme, language, maxConcurrentTransfers, defaultPort,
  connectionTimeoutSec, showHiddenFiles, passiveMode, autoReconnect,
  confirmOnDelete, dateFormat, maxTransferSpeedKBps, maxConnections,
  connectCardShadow, windowWidth, windowHeight, startMaximized.

`resetSetting(key)` sets `form = { ...form, [key]: DEFAULTS[key] }`.

A small `↺` button (class `.reset-btn`) was added inside every `<label>`
that has a resettable key, using `on:click|stopPropagation` to avoid
triggering the label's own click target. Styled as subtle/muted (opacity 0.45)
until hovered (full opacity, accent color), so it does not visually compete
with the setting control.

The version badge in the Settings panel footer was also updated from v1.7.4
to v1.7.5.

---

#79
Title: [Bug] "Edit site" button in Site Manager detail view renders as an oversized card
Labels: bug, ui
Version fixed: v1.7.5
Description:
After the right-panel centering fix (#76), the "Modifier le site" / "Edit site"
button in the Site Manager action row renders as a large card (roughly 100×100 px)
with a big pencil icon, dwarfing the "Connect" and "Delete site" buttons beside it.
Ref: _images/issues/v1.7.5/issue-31.jpg

Root cause: `.btn-secondary` lacked both `display: flex; align-items: center;
gap: 6px;` and a `.btn-secondary svg { width: 14px; height: 14px; }` rule.
The SVG inside the button therefore rendered at its intrinsic/default size
under WebKit-GTK instead of the 14 px used by `.btn-primary` and
`.btn-danger-outline`.

Solution (closing comment):
Fixed in v1.7.5 in SiteManager.svelte.

`.btn-secondary` updated to match the other action button classes:
  .btn-secondary {
    display: flex; align-items: center; gap: 6px;
    ...existing padding / border / color...
  }
  .btn-secondary svg { width: 14px; height: 14px; }

All three action buttons (Connect, Edit, Delete) now render at the same height
and with the same small icon size.

---

#80
Title: [Bug] Right-click context menu shows only "New folder" in tree view
Labels: bug
Version fixed: v1.7.5
Description:
In tree view, right-clicking any file or folder shows only the "New folder"
option (the empty-area context menu). The file-specific options — Rename,
Transfer, Cut, Copy, Paste, Delete — are never shown.
In list view the full context menu works correctly.
Ref: _images/issues/v1.7.5/issue-32.jpg

Root cause: the `.tree-row` divs had no `on:contextmenu` handler. Right-click
events bubbled up to the `.tree-list` container's parent, which was bound to
`handleBrowserContextMenu` (the empty-area handler that only offers "New
folder").

Solution (closing comment):
Fixed in v1.7.5 in FileBrowser.svelte.

Two changes:
1. `.tree-list` div: `on:contextmenu={handleBrowserContextMenu}` added so
   right-clicking an empty area in tree mode still shows "New folder".
2. Each `.tree-row` div: `on:contextmenu|stopPropagation` added. The handler
   builds an entry object from the node (`{ path, name, isDir, size, modTime }`)
   sets `treeSelected = node.path` and `selected = [ent]` (so the existing
   context menu logic — including the selection-aware transfer/cut/copy/delete
   checks — works without modification), then calls `handleFileContextMenu(e, ent)`.
The `stopPropagation` prevents the event from reaching the browser-context
handler, so only the file-specific menu is shown for non-empty rows.

---

#81
Title: [Bug] Tree view does not update after delete, paste, drag-drop, or new folder
Labels: bug
Version fixed: v1.7.5
Description:
In tree view, performing any file mutation — deleting a file, pasting a file
into a folder, moving a file by drag-and-drop within the same panel, or
creating a new folder — does not update the visible tree. The item remains
visible after deletion, the pasted file does not appear, etc. The user must
manually click Refresh, then switch back to list view and re-expand all folders
to see the current state.

Root cause: `doDeleteAll()`, `pasteToFolder()`, `handleDropOnFolder()`, and
`handleNewFolder()` all called `onRefresh()` after completing their operation.
`onRefresh()` only reloads the flat `entries` list used by the list view; it
has no effect on the tree state (`treeNodes`, `treeLoaded`, `treeChildrenMap`).

Solution (closing comment):
Fixed in v1.7.5 in FileBrowser.svelte.

Three new helpers were added:

`treeParentDir(entryPath)`: returns the parent directory of a given path.

`treeRefreshDir(dirPath)`: refreshes a single directory node in the tree
in-place — clears the cache entry for that directory, then collapses and
re-expands the corresponding `treeNodes` entry so only that subtree is
reloaded. For the root `/`, falls back to a full `enterTreeMode()`. Returns
immediately if the directory is not currently expanded (no visual change
needed).

`treeRemoveEntries(entries)`: removes a set of entries (and their descendant
nodes if they are directories) from `treeNodes` directly, then clears the
parent directory's cache entry. Used for deletes and moves where no new
content needs to be shown.

Each mutating operation was updated to branch on `treeMode`:

- **Delete** (`doDeleteAll`): calls `treeRemoveEntries(entries)` then clears
  `treeSelected` and `selected`. No network refetch required — the nodes are
  removed immediately.
- **Paste copy** (`pasteToFolder`): after `onRefresh()`, also calls
  `treeRefreshDir(destFolder)` to reload the destination directory.
- **Paste cut** (`pasteToFolder`): additionally calls `treeRemoveEntries` on
  the source entries before refreshing the destination.
- **New folder** (`handleNewFolder`): calls `treeRefreshDir(path)` instead
  of `onRefresh()`.
- **Drag-drop same panel** (`handleDropOnFolder`): calls `treeRemoveEntries`
  on the moved items, then `treeRefreshDir(destFolder)`.

---

#82
Title: [Bug] Rename via right-click does nothing in tree view
Labels: bug
Version fixed: v1.7.5
Description:
In tree view, right-clicking a file or folder and choosing "Rename" has no
visible effect. The inline rename input never appears.
In list view, rename works correctly (an inline text input replaces the file
name in the row).

Root cause: `startRename(entry)` sets `renamingEntry` which triggers an
`{#if renamingEntry?.path === entry.path}` block inside the `.file-list`
section. In tree mode the `.file-list` block is not rendered at all (replaced
by `.tree-list`), so `renamingEntry` was set but no input was ever shown.

Solution (closing comment):
Fixed in v1.7.5 in FileBrowser.svelte.

The `.tree-name` span in each tree row was updated to include the same
conditional input pattern used in the list view:

  <span class="tree-name">
    {#if renamingEntry?.path === node.path}
      <input
        class="tree-rename-input"
        type="text"
        bind:value={renameValue}
        on:click|stopPropagation
        on:keydown={(e) => {
          if (e.key === 'Enter') doRename(renamingEntry);
          if (e.key === 'Escape') renamingEntry = null;
        }}
        autofocus
      />
    {:else}
      {node.name}
    {/if}
  </span>

`doRename()` was also updated to handle tree mode: instead of calling
`onRefresh()`, it mutates `treeNodes` directly — updating the node's `name`
and `path` (and all descendant paths if it was a directory), updating
`treeSelected` if the renamed node was selected, and clearing the cache for
the parent directory. This keeps the tree in sync without a full rebuild.

---

1.7.6

#83
Title: [Feature] Native Linux package distribution (.deb, .rpm, AUR PKGBUILD) + GitHub Actions release automation
Labels: enhancement, build, linux
Version fixed: v1.7.6
Description:
GlideFTP was only available as a raw binary or AppImage, requiring users to
launch it from a terminal. No native package existed for Debian/Ubuntu (.deb),
Fedora/RHEL (.rpm), or Arch Linux (AUR). Each release also required fully
manual steps to build and publish all artefacts.

Solution (closing comment):
Implemented in v1.7.6.

**packaging/ directory (new):**
- `glideftp.desktop` - shared FreeDesktop desktop entry (Exec=glideftp,
  Categories=Network;FileTransfer;). Used by .deb, .rpm and the PKGBUILD.
- `PKGBUILD` - AUR binary package `glideftp-bin`. Sources the Linux .tar.gz
  release archive from GitHub releases. Installs binary to /usr/bin/glideftp,
  desktop entry and icon to standard XDG paths.
  sha256sums_x86_64=('SKIP') for local testing; update with real hash before
  publishing to AUR.
- `glideftp.spec` - RPM spec template. @VERSION@ and @CHANGELOG_DATE@ are
  replaced at build time by docker/build-rpm.sh.

**Docker containers:**
- `docker/Dockerfile.rpm` - Fedora 40 minimal image with rpm-build +
  rpmdevtools. No Go/Node — binary is pre-built and mounted from host.
- `docker/build-deb.sh` - runs inside glideftp-debian-builder (Ubuntu 22.04,
  already used for the Debian AppImage). Builds the .deb with dpkg-deb.
  Output: build/bin/linux/GlideFTP-Linux-v{version}.deb
- `docker/build-rpm.sh` - runs inside glideftp-rpm-builder (Fedora 40).
  Calls rpmdev-setuptree + rpmbuild -bb (binary RPM, no compilation).
  Output: build/bin/linux/GlideFTP-Linux-v{version}.rpm

**make.sh extended (new targets):**
  ./make.sh deb    <version>   .deb package (Docker - Ubuntu 22.04)
  ./make.sh rpm    <version>   .rpm package (Docker - Fedora 40)
  ./make.sh packages <version> .deb + .rpm
  ./make.sh -h                 show full help with all targets and version info
  Version can be passed as 2nd argument or read from a VERSION file at root.
  ./make.sh all also builds .deb + .rpm (requires version).

**create-archive.sh modified:**
  The Linux binary archives (.tar.gz and .tar) now include glideftp.desktop
  and glideftp.png alongside the binary and README.md. This is required so
  the AUR PKGBUILD can extract all three files from a single source archive.

**.github/workflows/release.yml (new):**
  Trigger: push of a tag matching v[0-9]+.[0-9]+.[0-9]+ or manual dispatch.
  Runner: ubuntu-22.04.
  Steps: checkout, set up Go 1.25 + Node 20, install system deps (webkit2gtk,
  mingw-w64, imagemagick), install Wails CLI, build Linux binary, build
  Windows binary, build Debian AppImage (Docker), build .deb (Docker), build
  .rpm (Docker), create distribution archives, create draft GitHub release with
  all artefacts and release notes from v{version}.md.
  Docker layer caching via GitHub Actions cache (scope=debian-builder,
  scope=rpm-builder) to speed up repeated runs.
  Note: Arch AppImage is NOT built in CI (requires Arch Linux system libs).
  Build it locally with ./make.sh appimage-arch then upload to the draft
  manually before publishing.

**Dependency names per distro:**
  Debian/Ubuntu : libwebkit2gtk-4.1-0
  Fedora/RHEL   : webkit2gtk4.1
  Arch Linux    : webkit2gtk-4.1

**AUR publication (one-time setup):**
  git clone ssh://aur@aur.archlinux.org/glideftp-bin.git
  cp packaging/PKGBUILD glideftp-bin/
  # Update pkgver and sha256sums_x86_64 with sha256sum of the release archive
  makepkg --printsrcinfo > .SRCINFO
  git add PKGBUILD .SRCINFO && git commit -m "Release v{version}" && git push
  Note: AUR account registration is sometimes temporarily disabled (anti-spam).

**Local PKGBUILD test on Arch/Garuda:**
  ./make.sh linux && ./create-archive.sh -p linux -t gz {version}
  mkdir /tmp/pkgt && cp packaging/PKGBUILD /tmp/pkgt/
  cp GlideFTP-Linux-v{version}.tar.gz /tmp/pkgt/glideftp-bin-{version}.tar.gz
  cd /tmp/pkgt && makepkg -si
  # Uninstall: sudo pacman -R glideftp-bin

---

#84
Title: [Feature] Select individual sites to export in Site Manager
Labels: enhancement, ui
Version fixed: v1.7.6
Description:
Clicking "Export sites" in the Site Manager immediately shows the
with/without-password dialog and always exports all saved sites. There is no
way to export only a subset of sites.
Ref: _images/issues/v1.7.6/issue-34.png

Solution (closing comment):
Implemented in v1.7.6 in SiteManager.svelte and app.go.

**Flow:**
Clicking "Export sites" now enters a selection step first:
1. exportSelectMode = true, all sites pre-selected (exportSelectedIds = Set of
   all site IDs).
2. Left panel: "Tout sélectionner" / "Select all" toggle replaces the
   "+ New site" button. Each site row shows a checkbox on the left.
   Clicking a site row toggles its selection. Clicking the top toggle
   selects all or deselects all (three visual states: all checked, partial,
   none).
3. Right panel (no-selection area): shows "Sélectionner les sites à exporter"
   / "Select sites to export" + two centered buttons: Valider / Validate
   (disabled when 0 sites selected) and Annuler / Cancel.
4. Valider: exits select mode and opens the existing export dialog
   (with or without passwords).
5. Annuler: exits select mode with no action.

**Frontend — SiteManager.svelte:**
New state:
  exportSelectMode bool
  exportSelectedIds Set<string>
  exportAllChecked  reactive (size === sites.length && sites.length > 0)
  exportNoneChecked reactive (size === 0)

New functions:
  openExportDialog()     — now enters exportSelectMode instead of showExportDialog
  cancelExportSelect()   — resets exportSelectMode + exportSelectedIds
  confirmExportSelect()  — exits select mode, opens showExportDialog
  toggleExportSite(id)   — toggles one site in exportSelectedIds
  toggleExportAll()      — selects all or deselects all

doExportPlain() and doExportEncrypted() now call the new Selected variants
passing [...exportSelectedIds] as the IDs list.

CSS: .export-checkbox (16px square, accent fill when checked, partial border
when intermediate), .export-selectable (cursor pointer on site rows in select
mode).

**Go — app.go:**
Two new methods alongside the existing ones (kept for compatibility):
  ExportSitesPlainSelected(ids []string) error
    Same as ExportSitesPlain but filters to the given site IDs.
  ExportSitesEncryptedSelected(passphrase string, ids []string) error
    Same as ExportSitesEncrypted but filters to the given site IDs.
  Both fall back to exporting all sites if ids is empty.

Helper:
  filterSitesByIDs(all []sites.Site, ids []string) []sites.Site
    Returns only sites whose ID appears in ids. Returns all if ids is empty.

**JS bindings:**
ExportSitesPlainSelected and ExportSitesEncryptedSelected added manually to
frontend/wailsjs/go/main/App.js.

**i18n:**
  exportSelectTitle : "Sélectionner les sites à exporter" / "Select sites to export"
  exportSelectAll   : "Tout sélectionner" / "Select all"
  exportValidate    : "Valider" / "Validate"

---

#85
Title: [Bug] Cannot validate site export when a site is already selected in Site Manager
Labels: bug, ui
Version fixed: v1.7.6
Description:
In Site Manager, if the user clicks on an already saved site (its detail
panel opens on the right) and then clicks "Export sites", the export
selection panel (checkboxes + "Select sites to export" + Validate/Cancel
buttons, added in #84) does not appear. The previously opened site's detail
panel (Connect / Edit / Delete buttons) stays displayed instead, so the user
has no way to validate the export.
Ref: _images/issues/v1.7.6/issue-35.png

Solution (closing comment):
Fixed in v1.7.6 in SiteManager.svelte.

**Root cause:**
The right panel template is a single if/else-if/else chain:
  {#if editMode} edit form
  {:else if selectedSite} site detail view (Connect/Edit/Delete)
  {:else} no-selection panel — shows the export-select UI when
          exportSelectMode is true
  {/if}

openExportDialog() (called by the "Export sites" button) only set
exportSelectMode = true and pre-filled exportSelectedIds — it never reset
selectedSite (or editMode). So if a site was already selected (or being
edited) when the button was clicked, the {:else if selectedSite} branch kept
rendering its detail/edit view instead of falling through to the
{:else} branch that hosts the Validate/Cancel export panel.

**Fix:**
openExportDialog() now also resets editMode = false and selectedSite = null
before entering exportSelectMode, guaranteeing the no-selection/export-select
panel is shown regardless of what was open in the right panel beforehand.

---

#86
Title: [UI] Replace Dark/Light theme toggle emoji icons with SVG icons
Labels: enhancement, ui
Version fixed: v1.7.6
Description:
In Settings > Appearance, the theme toggle ("Sombre"/"Dark" and
"Clair"/"Light") used raw emoji (🌙 and ☀️) as icons. Emoji rendering is
inconsistent across platforms/fonts (color, weight, alignment vs the rest of
the app's line-icon SVGs). Requested to replace them with clean line-style
SVG icons (crescent moon / sun with rays) matching the icon style used
elsewhere in the app (e.g. the accent color picker icon), same for both
languages since only the icon changes, not the label.

Solution (closing comment):
Implemented in v1.7.6 in SettingsPanel.svelte.

**Frontend — SettingsPanel.svelte:**
Replaced the 🌙 / ☀️ emoji in the theme `.toggle-group` buttons with inline
SVGs (viewBox 24x24, fill none, stroke currentColor, stroke-width 2 — same
convention as the existing accent-color-picker icon):
  Dark  : crescent moon path
          `<path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>`
  Light : sun (circle + 8 rays)
          `<circle cx="12" cy="12" r="5"/>` + 8 `<line>` rays

**CSS:**
  .toggle-btn now `display: flex; align-items: center; gap: 6px` so the icon
  and label align on one line (previously text-only, no flex needed).
  New `.theme-icon` class: 14x14px, `flex-shrink: 0`.

No i18n changes — themeDark / themeLight labels (Sombre/Dark, Clair/Light)
are unchanged, only the icon markup changed.

---

#87
Title: [Feature] Setting to prevent "Manage sites" from closing on outside click
Labels: enhancement, settings
Version fixed: v1.7.6
Description:
Clicking anywhere outside the "Manage sites" modal (on the backdrop) closes
it immediately. It's easy to misclick while filling in a site's details and
lose everything just entered. Requested a setting to control this: when
enabled (default), clicking outside closes the modal like today; when
disabled, only the ✕ button closes it.

Solution (closing comment):
Implemented in v1.7.6 in settings.go, SettingsPanel.svelte and
SiteManager.svelte.

**Go — internal/settings/settings.go:**
New field:
  CloseSiteManagerOnClickOutside bool `json:"closeSiteManagerOnClickOutside"`
Default: true (preserves current behavior for existing users - an old
settings.json without the key unmarshals over Default(), so it still lands
on true).

**Frontend — SettingsPanel.svelte:**
New toggle in the "Interface" section, right under "Connection card accent
shadow":
  closeSiteManagerOnClickOutside — standard `.sw` switch button (WebKit-GTK
  pattern: button-based toggle, not a hidden checkbox), with its own reset
  (↺) button. Added to DEFAULTS map (true) so reset restores the default.

**Frontend — SiteManager.svelte:**
The modal backdrop click handler now checks the setting before closing:
  on:click|self={() => { if ($settings?.closeSiteManagerOnClickOutside !== false) onClose(); }}
`!== false` so the modal still closes on outside click while settings are
loading (undefined) or for any user who already has the value `true` -
only an explicit `false` blocks the outside-click close. The ✕ button in
the modal header is untouched and always closes the modal regardless of
this setting.

**i18n:**
  closeSiteManagerOnClickOutside :
    FR "Fermer "Gérer les sites" en cliquant à côté"
    EN "Close "Manage sites" by clicking outside"

---

#88
Title: [Feature] Setting to require a double-click on ".." to go to the parent folder
Labels: enhancement, settings
Version fixed: v1.7.6
Description:
The ".." row (and the "parent folder" toolbar arrow) navigates to the
parent folder on a single click. Requested a setting to require a
double-click instead, to avoid accidental navigation. Must apply to both
panels (local and remote) and both view modes (list view and tree view).

Solution (closing comment):
Implemented in v1.7.6 in settings.go, SettingsPanel.svelte and
FileBrowser.svelte.

**Go — internal/settings/settings.go:**
New field:
  DoubleClickNavigateUp bool `json:"doubleClickNavigateUp"`
Default: false (preserves current single-click behavior).

**Frontend — SettingsPanel.svelte:**
New toggle in the "Interface" section, right after "Close Manage sites by
clicking outside":
  doubleClickNavigateUp — standard `.sw` switch with its own reset (↺)
  button. Added to DEFAULTS map (false).

**Frontend — FileBrowser.svelte:**
FileBrowser is instantiated once per panel (local/remote), so the setting
applies to both automatically — no per-side wiring needed.

New reactive flag:
  $: dblClickUp = $settings?.doubleClickNavigateUp === true;

Two places gated by dblClickUp (both are the only "go to parent folder"
affordances, covering list view and tree view respectively — tree view has
no ".." row of its own):

1. List view ".." row (`.parent-row`):
   New handleParentClick() replaces the inline single-click handler:
     - dblClickUp = false (default): click navigates up immediately
       (parentFocused = false; onNavigateUp()) — unchanged behavior.
     - dblClickUp = true: click only sets parentFocused = true (highlights
       the row, same visual state as ArrowUp keyboard navigation) without
       navigating. on:dblclick={onNavigateUp} still navigates on double-click
       regardless of the setting.

2. Toolbar "parent folder" arrow button (browser-header, shown in both list
   and tree view since it's outside the {#if treeMode} block):
     on:click={() => { if (!dblClickUp) onNavigateUp(); }}
     on:dblclick={onNavigateUp}
   Same logic: single click navigates only when the setting is off; double
   click always navigates.

Keyboard navigation (ArrowUp to focus "..", Enter to navigate) is
unchanged — Enter is an explicit activation, treated the same as a
double-click regardless of the setting.

**i18n:**
  doubleClickNavigateUp :
    FR "Double-cliquer sur ".." pour aller au dossier parent"
    EN "Double-click ".." to go to parent folder"

---

#89
Title: [Feature] Search in the current folder (with recursive subfolder search)
Labels: enhancement, ui
Version fixed: v1.7.6
Description:
Requested a search feature in the file browser: a magnifying-glass button
between the tree-view toggle and the refresh button, for both the local and
remote panels, to search inside the current folder ("."). Should also
support searching recursively through all subfolders when possible, and
must work the same in both list view and tree view. Follow-up request:
recursive subfolder search should be the default (on), both locally and
remotely, not something the user has to switch on each time.

Solution (closing comment):
Implemented in v1.7.6 across local.go, app.go, connection.js, App.svelte and
FileBrowser.svelte.

**Go — internal/fs/local.go:**
  Search(dir, query string, recursive, showHidden bool) ([]FileEntry, error)
    Non-recursive: filters dir's direct children by case-insensitive
    substring match. Recursive: walks every subdirectory (searchWalk),
    same matching rule. Capped at 500 results (searchResultLimit) so a
    huge local tree can't hang the UI.

**Go — app.go:**
  LocalSearch(path, query string, recursive bool) ([]localfs.FileEntry, error)
    Thin wrapper around localfs.Search using the current ShowHiddenFiles
    setting.
  RemoteSearch(path, query string, recursive bool) ([]connection.RemoteFileEntry, error)
    Recursive walk (remoteSearchWalk) built on the existing a.connMgr.ListDir
    (already has its own per-call timeout/watchdog). Each subdirectory is a
    network round-trip; a permission error on one subdirectory doesn't abort
    the rest of the search (best-effort). Capped at 500 results
    (remoteSearchResultLimit) for the same reason as the local search.
  JS bindings regenerated via `wails generate module` (LocalSearch /
  RemoteSearch added to frontend/wailsjs/go/main/App.{js,d.ts}).

**Frontend — connection.js / App.svelte:**
  localSearch(path, query, recursive) / remoteSearch(path, query, recursive)
  wrap LocalSearch/RemoteSearch. Passed into each FileBrowser instance as the
  onSearch prop, same pattern as onMkDir/onDelete/onRename.

**Frontend — FileBrowser.svelte:**
New toolbar button (magnifying glass) between the tree-view toggle and the
refresh button, in the shared browser-header — so it appears identically in
both panels and is unaffected by list/tree mode.

Search bar (inline row, like the existing new-folder row): text input +
recursive toggle button + spinner (while a recursive backend search is in
flight) + close button. Esc or the close button exits search mode.

State: searchMode, searchQuery, searchRecursive (default `true` - see
follow-up below), searching, searchResults, debounced 350ms on input.

Non-recursive search filters the already-loaded `entries` prop reactively -
instant, no backend call. Recursive search calls onSearch(path, query, true)
with a request-id guard (searchReqId) so a stale response from an earlier
keystroke can't overwrite a newer one.

Unified rendering for both view modes: while search results are non-empty
(showSearchResults), they replace whichever view is currently shown
({#if showSearchResults} ... {:else if treeMode} ... {:else} list view
{/if}) - list view and tree view both get the same flat results list, so no
separate implementation was needed for tree view. Each result row shows the
name and, in recursive mode, the relative path of its containing subfolder.
Double-click on a folder result navigates into it and closes search;
double-click on a file result transfers it (transferEntry). Right-click
keeps the normal Rename/Transfer/Delete context menu, drag-and-drop out of
the results list works too (draggable + handleDragStart).

Rubber-band selection and Shift-click range-select were updated to read
from the search results list (sortedSearchResults) instead of the normal
sortedEntries while showSearchResults is true, so multi-select still works
correctly in search results.

Delete/rename from within search results refresh via
refreshAfterSearchMutation() (onRefresh() + re-run the recursive search if
one is active) instead of the tree-specific or list-specific refresh paths.

Search automatically closes when the current folder changes
(`$: if (path) closeSearch();`), since a search always applies to whichever
folder was current when it was opened.

**Follow-up - recursive search on by default:**
searchRecursive now initializes to `true` instead of `false`, so opening
the search bar starts in recursive mode (both local and remote panels,
since it's the same component/prop for both) - matches the current folder's
subfolders being searched immediately without an extra click. Users can
still toggle it off for a search scoped to just the current folder.

**i18n:**
  search            : FR "Rechercher" / EN "Search"
  searchPlaceholder : FR "Rechercher dans ce dossier..." / EN "Search in this folder..."
  searchRecursive   : FR "Rechercher aussi dans les sous-dossiers" / EN "Search in subfolders too"
  searchResults     : FR "Résultats de recherche" / EN "Search results"
  noSearchResults   : FR "Aucun résultat" / EN "No results"

---

#90
Title: [Feature] Sound notification when the transfer queue finishes
Labels: enhancement, settings, transfers
Version fixed: v1.7.7
Description:
Requested a sound notification played when transfers finish, with an
on/off setting and a choice of sound (a dropdown of available sounds plus
a speaker button to preview one). Sound only, no OS/desktop notifications.
No sounds were provided by the user — asked to create them. Follow-up:
must work for both uploads and downloads, and the sound must play only
once the whole transfer queue is empty, not once per individual file.

Solution (closing comment):
Implemented in v1.7.7 across settings.go, a new generated-sounds pipeline,
sound.js, transfers.js and SettingsPanel.svelte.

**Go — internal/settings/settings.go:**
Two new fields:
  NotificationSoundEnabled bool   `json:"notificationSoundEnabled"`
  NotificationSound        string `json:"notificationSound"`
Defaults: false / "chime". Plain JSON pass-through — no Go-side audio
code at all, playback is 100% frontend.

**Sound generation — scripts/generate_notification_sounds.py:**
Pure-stdlib Python (wave/struct/math/base64, no external deps) synthesizes
four short, soft tones — soft attack + exponential decay so none of them
are jarring:
  chime — two soft ascending bell notes (C6 → E6), slightly overlapping
  bell  — a single warm, long-decaying tone (A5) with inharmonic overtones
  pop   — a very short wood-block/marimba-like blip (C5)
  glass — an airy, bright descending two-note (E6 → C6)
Writes frontend/src/assets/sounds/*.wav (reference copies, for listening)
AND frontend/src/utils/sounds.generated.js (the same four sounds as
`data:audio/wav;base64,...` strings) — see the WebKitGTK issue below for
why the .wav files themselves are not imported by the app.

**WebKitGTK Linux gotcha (the actual bulk of this issue's debugging):**
A plain imported .wav asset URL failed completely silently in the real
app on Linux — no sound, no JS error, no crash — while the exact same
code played fine when the built page was opened manually in a real
browser (Chrome) at the wails-dev debug URL. Root cause: on Linux, Wails'
native window always loads the frontend through its own custom `wails://`
URI scheme (true in both `wails dev` and the packaged binary — the
`http://localhost:34115` printed by `wails dev` is only for manually
opening the page in an external browser to debug, not what the app
window itself navigates to). WebKitGTK's HTML5 `<audio>`/`<video>` loader
only recognizes a fixed allowlist of URI schemes (http/https/file/data/
blob/...) before ever touching GStreamer — `wails://` isn't in that list,
so the media element fails instantly with
`MediaPlayerPrivateGStreamer.cpp:1554:loadingFailed: ... FormatError`,
confirmed by `GST_DEBUG=3,webkitwebsrc:6,souphttpsrc:6,typefind:5`
producing zero output from any of those categories (the failure happens
before a GStreamer pipeline is even built). Ruled out along the way,
none of which fixed it: an unreferenced `new Audio()` being garbage
collected before playback (fixed separately by caching instances, real
bug but not the blocker), `WEBKIT_DISABLE_SANDBOX=1`, and
`WEBKIT_DISABLE_COMPOSITING_MODE=1` (GPU/compositing was a red herring —
confirmed via an isolated WebKitGTK MiniBrowser test). Fix: embed each
sound as a `data:` URI instead of a plain asset URL — a scheme WebKit's
media loader does accept. Vite 3 (pinned by this project) has no
dev-server-side asset inlining (`?inline` and `assetsInlineLimit` only
apply to `vite build`, not `vite dev`), hence generating the base64 JS
module ahead of time in Python rather than relying on Vite's own
inlining. Any future audio/video element must follow this same
`sounds.generated.js`-style `data:` URI pattern on Linux.

**Frontend — utils/sound.js:**
  NOTIFICATION_SOUNDS = [{id, label, url}, ...] read from
  sounds.generated.js's SOUND_DATA_URLS.
  playNotificationSound(id) — one cached Audio instance per sound (kept
  alive for the app's lifetime, see garbage-collection note above),
  audio.currentTime = 0 then audio.play(), errors logged via
  console.error instead of being swallowed.

**Frontend — stores/transfers.js (queue-drain, not per-file):**
  hadActiveJobs module-level flag + a subscription on the `transfers`
  store computing `active = list.some(j => j.status is pending/running)`.
  The sound plays only on the transition from hadActiveJobs=true to
  active=false — i.e. once, right when the last queued job (upload or
  download, mixed batches included) finishes, never once per file.
  Gated on settings.notificationSoundEnabled.

**Frontend — SettingsPanel.svelte:**
New "Notifications" section (between Transfers and Connection):
  - on/off switch (`.sw` pattern) — notificationSoundEnabled
  - when on: a `<select>` dropdown of the 4 sound names (i18n-labeled)
    bound to notificationSound, plus a speaker-icon button that calls
    playNotificationSound(form.notificationSound) to preview
  Both added to the DEFAULTS map for the ↺ reset buttons. `<select>` was
  used deliberately — a pattern already proven safe on WebKit-GTK
  elsewhere in this app (ConnectionBar/SiteManager), unlike hidden
  checkboxes or native number spinners.

**i18n:**
  notifications            : FR "Notifications" / EN "Notifications"
  notificationSoundEnabled : FR "Jouer un son quand un transfert est terminé"
                              EN "Play a sound when a transfer finishes"
  notificationSound        : FR "Son de notification" / EN "Notification sound"
  testSound                : FR "Tester le son" / EN "Test sound"
  soundChime / soundBell / soundPop / soundGlass :
    FR "Carillon" / "Cloche" / "Pop" / "Verre"
    EN "Chime" / "Bell" / "Pop" / "Glass"

---

#91
Title: [Bug] Delete key does nothing after a rubber-band (click-drag) selection
Labels: bug, ui
Version fixed: v1.7.7
Description:
Drag a selection box over several files/folders (rubber-band selection),
then press Delete: nothing happens. Ctrl+click to select the same items
and press Delete works fine — only the click-drag selection is affected.

Solution (closing comment):
Fixed in v1.7.7 in FileBrowser.svelte.

**Root cause:**
The panel div (`tabindex="-1"`) carries the `on:keydown={handlePanelKeydown}`
listener that Delete/F2/arrow-key navigation depend on — it only fires
while that div actually has DOM focus. A normal row click (`handleClick`,
Ctrl+click and Shift+click included) explicitly calls
`panelEl?.focus({ preventScroll: true })` after updating `selected`, which
is what makes Delete work afterwards. The rubber-band drag-select
(`handleListMouseDown` / its `onMove`/`onUp` handlers) correctly builds up
`selected` as the box is dragged, but never called `panelEl.focus()` —
so keyboard focus stayed wherever it was before the drag started, and the
keydown listener never received the Delete press.

**Fix:**
`onUp()` (fired on mouseup, ending the drag) now also calls
`panelEl?.focus({ preventScroll: true })`, matching what `handleClick`
already does — after both a click-drag and a plain click/Ctrl+click/
Shift+click selection, the panel has focus and Delete/F2/arrow keys work.

---

#92
Title: [Feature] Reorder saved sites via drag-and-drop in Site Manager
Labels: enhancement, ui
Version fixed: v1.7.7
Description:
Requested the ability to reorder saved sites in Site Manager via
drag-and-drop, toggled by a small square button to the right of
"+ Nouveau site". Icon choice left to Claude.

Solution (closing comment):
Implemented in v1.7.7 across sites.go, app.go and SiteManager.svelte.

**Go — internal/sites/sites.go:**
No new field on Site — order is simply the array position in the
existing []Site slice/sites.json, same as it already was.
  Reorder(orderedIDs []string) error
    Rebuilds m.sites to match orderedIDs. Any existing site whose ID is
    missing from orderedIDs (stale/partial payload from the frontend) is
    kept and appended at the end instead of being silently dropped. Saves
    via the existing save() (passwords stripped, as always).

**Go — app.go:**
  ReorderSites(orderedIDs []string) error — thin wrapper around
  siteMgr.Reorder. JS bindings regenerated via `wails generate module`.

**Frontend — SiteManager.svelte:**
New state: reorderMode (bool), dragSiteId, dragOverSiteId.

New square toggle button next to "+ Nouveau site" — both now wrapped
together in `.site-list-toolbar` so they sit side by side, sharing the
toolbar's bottom border. Clicking it flips reorderMode. Icon swaps with
the state so the button's meaning is unambiguous: up/down chevrons when
off ("start reordering"), a checkmark when on ("done reordering, tap to
confirm/exit") — the title tooltip switches between the two matching
i18n keys as well.

Follow-up request: swap to a "validate" icon while reorder mode is
active, so the button reads as "click to confirm" rather than repeating
the same icon regardless of state.

While reorderMode is true, each site row (.site-item):
  - gets draggable={true} and a 6-dot grip icon (.drag-handle) on the
    left, for visual affordance
  - a dashed outline (.drag-over) highlights the row currently being
    dragged over
  - normal click-to-select is disabled (`if (reorderMode) return;` in
    the row's click handler) so a drag can't accidentally open a site's
    detail view instead of reordering it

Drag handlers (same intra-app HTML5 drag-and-drop pattern already used
by FileBrowser's own row dragging — proven safe on WebKit-GTK):
  handleSiteDragStart — stores the dragged site's id
  handleSiteDragOver  — preventDefault + dropEffect='move', sets
                         dragOverSiteId for the highlight
  handleSiteDrop      — splices the dragged site into its new index in
                         the local `sites` array (instant visual
                         feedback), then calls
                         ReorderSites(sites.map(s => s.id)) to persist
  handleSiteDragEnd   — clears drag state (fires even if dropped outside
                         a valid target)

openExportDialog() now also sets reorderMode = false, since export-select
and reorder mode both repurpose the same toolbar/row area and can't be
active at the same time. No explicit reset needed on modal close —
SiteManager is fully destroyed/recreated ({#if showSiteManager} in
App.svelte) each time the modal is closed and reopened.

**i18n:**
  reorderSites     : FR "Réorganiser les sites (glisser-déposer)"
                     EN "Reorder sites (drag and drop)"
  reorderSitesDone : FR "Terminer la réorganisation"
                     EN "Done reordering"

---

#93
Title: [Bug] Ugly/illogical layout for the delete-site confirmation in Site Manager
Labels: bug, ui
Version fixed: v1.7.7
Description:
Clicking "Supprimer le site" showed the confirmation text and Oui/Non
buttons crammed into the same row as Connecter/Modifier le site — the
text ended up off to the side and Oui/Non wrapped underneath only
"Modifier le site" instead of the whole row, not centered. Requested
layout: Connecter + Modifier le site on their row, then centered below
"Êtes-vous sûr de vouloir supprimer ce site ?", then centered below that
the Oui/Non buttons. Applies to both languages (FR/EN).
Ref: _images/issues/v1.7.7/issue-36.png

Solution (closing comment):
Fixed in v1.7.7 in SiteManager.svelte — template/CSS only, no state or
handler changes (confirmDeleteId, deleteSite() unchanged).

**Root cause:**
The confirmation text and Oui/Non buttons were rendered as extra items
inside the *same* `.view-actions` flex row as Connecter/Modifier le
site, instead of on their own row(s) — `flex-wrap` then broke the row
unevenly depending on button widths.

**Fix:**
`.site-view` is already `display: flex; flex-direction: column;
align-items: center` (each direct child is centered and evenly
gapped automatically). The markup is now three separate stacked blocks:
  1. `.view-actions` — Connecter + Modifier le site only; "Supprimer le
     site" is hidden while confirming (`{#if confirmDeleteId !==
     selectedSite.id}`)
  2. `<p class="confirm-text">` — the "Êtes-vous sûr..." message alone,
     `margin: 0; text-align: center`
  3. a second `.view-actions` div — Oui / Non only
Rendered only while `confirmDeleteId === selectedSite.id`.

---

#94
Title: [Feature] Export / import all Settings as a JSON file
Labels: enhancement
Version fixed: v1.7.7
Description:
Requested the ability to export the entire Settings page (all app
settings) to a JSON file and import it back, so the exact same
configuration can be carried over to a different PC.

Solution (closing comment):
Implemented in v1.7.7 across app.go and SettingsPanel.svelte.

**Go — app.go:**
  ExportSettings() error — SaveFileDialog ("glideftp-settings.json"),
  writes the current in-memory a.appSettings as indented JSON. No
  passwords or per-site data involved — this is the Settings page only,
  unrelated to the existing sites export/import (.gfe) feature.
  ImportSettings() (*settings.Settings, error) — OpenFileDialog, reads
  the file, unmarshals onto settings.Default() (so a partial/older
  export doesn't zero out fields missing from the file), immediately
  applies it (queue.SetWorkers/SetSpeedLimit) and persists it to disk
  via Settings.Save(), then returns the resolved struct so the frontend
  can sync its store without a restart.

**Frontend — SettingsPanel.svelte / stores/settings.js:**
  exportSettings()/importSettings() call the two bindings above.
  importSettings() calls the existing saveSettings(imported) helper
  (applies theme/locale/accent immediately) and updates the local
  `form` so the panel reflects the imported values right away.

**UI placement (iterated per follow-up review):**
First pass put two labeled buttons under a new "Import / Export"
section in the panel body. Moved per follow-up to two icon-only round
buttons (`.io-icon-btn`, 30px, border-radius 50%) in the panel footer,
directly after the "vX.Y.Z" version badge (a `.footer-spacer` div
pushes "Enregistrer" to the right as before); `title` keeps the
"Exporter/Importer les paramètres" tooltip on hover.
Second follow-up: the down-arrow icon must mean Import and the
up-arrow Export (opposite of the initial download/upload-icon choice)
— fixed by swapping only the two <svg> icons, button positions
unchanged (Export stays left of Import). The same swap was applied to
the analogous Export/Import buttons in SiteManager.svelte's "Gérer les
sites" header, for visual consistency between the two features.

**i18n:** exportSettings, importSettings, settingsImported (FR/EN).

**wailsjs bindings:** ExportSettings/ImportSettings/ExpandLocalPath (see
#95) added manually to frontend/wailsjs/go/main/App.{js,d.ts} at their
alphabetical position, matching the generator's own convention — this
session did not run a full `wails build`; the user's own local
`./make.sh` build afterwards confirmed no drift.

---

#95
Title: [Bug] Environment variables (%USERNAME%, $USER, ~) not expanded in local paths
Labels: bug
Version fixed: v1.7.7
Description:
Typing a path containing an environment variable — e.g.
C:\Users\%USERNAME%\Desktop, or $USER/$HOME, or ~ — into the "Default
local directory" setting did nothing: on next launch the local panel
kept showing/using the previous directory instead of resolving the
variable. Also reported: after saving a new "Default local directory"
(typed by hand, or picked via the "..." browse button), the change
only took effect after fully closing and reopening the app.

Solution (closing comment):
Two independent fixes in v1.7.7.

**1) Environment variable / ~ expansion — internal/fs/local.go, app.go:**
  ExpandPath(path string) string — new helper. Expands a leading ~ or
  ~/ to the home directory, then Windows-style %VAR% (regexp
  `%([^%]+)%`, looked up via os.LookupEnv) and Unix-style $VAR/${VAR}
  (via os.Expand). A reference to an unset variable is left untouched
  rather than collapsed to "", so a typo stays visible/debuggable
  instead of silently breaking navigation.
  Exposed to the frontend as app.ExpandLocalPath(path) — a pure string
  transform, never touches the filesystem.

  Deliberately NOT applied inside ListDir or the shared onNavigate path
  used for every directory click — only at the two places where the
  text is actually hand-typed:
    - stores/connection.js initLocalDir() — resolves the "Default
      local directory" setting at startup (and on live-apply, below)
    - FileBrowser.svelte confirmEditPath() — the editable local path
      bar, gated on `side === 'local'` (a remote FTP/SFTP path like
      "$logs" is a literal server directory name and must never be
      touched)
  This avoids a real risk: expanding on every ListDir call would mangle
  navigation into a real folder whose name happens to contain a $ or %
  character (e.g. Windows' own "$Recycle.Bin").

**2) Default local directory now applies live — App.svelte:**
  handleSettingsSaved(newSettings) now compares newSettings.
  defaultLocalDir against a tracked lastDefaultLocalDir; if it changed,
  it calls initLocalDir(newValue) (which also does the expansion above)
  to navigate the local panel immediately instead of only refreshing
  the current path. Falls back to a plain refreshLocal() when the
  default-dir setting itself didn't change (e.g. only showHiddenFiles
  changed), so unrelated settings saves don't yank the user's browsing
  location.

Verified with a temporary Go test exercising $HOME, ~, %USERNAME%
(env var set manually) and an unset-variable reference — all resolved
correctly, the unset case left untouched as designed.

---

#96
Title: [UI] Replace native Wails/WebKit alert() popups with a themed modal
Labels: enhancement, ui
Version fixed: v1.7.7
Description:
The success/error messages after exporting or importing Settings and
Sites used the browser's/Wails' native alert() — an unstyled
"JavaScript - wails://wails/" box with a plain "Fermer" button, not
matching the app's theme. Requested a themed pop-up instead, translated
FR/EN per the current language, with the pop-up's border in the accent
color. Follow-up: remove the checkmark icon and make the message read
like a large title instead of small body text.

Solution (closing comment):
New global notification system, implemented in v1.7.7.

**frontend/src/stores/notify.js:**
  notification writable store — { message, type: 'success'|'error' } |
  null.
  notify(message, type = 'success') / closeNotify() — thin setters.

**frontend/src/components/NotifyModal.svelte:**
Centered overlay + box, mounted once at the root in App.svelte (so it
always renders above any other panel/modal — z-index 5000, higher than
SiteManager's 700 and SettingsPanel's 400). 2px border in var(--accent)
plus a matching accent-tinted outer glow (box-shadow). use:trapFocus,
per the existing WebKit-GTK popup pattern. Closes on backdrop click, on
the "Fermer"/"Close" button (i18n key `close`, already existed), or on
Escape — wired into App.svelte's existing global handleKeydown, checked
before the Settings/SiteManager Escape handling so the topmost popup
always closes first.
Message text: 19px/700 weight, centered, no icon (removed per
follow-up) — reads as a title rather than an alert body. Colored
var(--danger) when type is 'error'; default text color otherwise (no
green tint on success, kept neutral per the reference screenshot).

**Callers switched from alert() to notify():**
  SettingsPanel.svelte — exportSettings/importSettings catch blocks
    (type 'error') and the "Paramètres importés" success message.
  SiteManager.svelte — all site-connect failures (now via the existing
    `connectError` i18n key instead of a hardcoded "Connection failed"
    fallback string), the export/import error catches, and the "N
    sites importés" success message.
No alert()/confirm() call remains anywhere in frontend/src (verified by
grep after the change) — every one of these now goes through the same
notify()/NotifyModal path.
